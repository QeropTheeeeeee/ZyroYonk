local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local player = Players.LocalPlayer

-- ================== EXECUTOR DETECTION ==================
local function getExecutorName()
    if identifyexecutor then
        local success, name = pcall(identifyexecutor)
        if success and name and name ~= "" then return name end
    end
    if getexecutorname then
        local success, name = pcall(getexecutorname)
        if success and name and name ~= "" then return name end
    end
    if syn and syn.crypt and syn.crypt.custom then return "Synapse" end
    if krnl then return "Krnl" end
    if fluxus then return "Fluxus" end
    if electron then return "Electron" end
    if is_sirhurt_closure then return "SirHurt" end
    if getgenv and getgenv().IS_SCRIPT_WARE then return "ScriptWare" end
    if getgenv and getgenv().IS_VEGA_X then return "Vega X" end
    if getgenv and getgenv().IS_DELTA then return "Delta" end
    if getgenv and getgenv().IS_CODE_X then return "Code X" end
    return "Unknown Executor"
end

local executorName = getExecutorName()

-- ================== ROBUST LIVE PING (MULTIPLE METHODS) ==================
local function getPing()
    local success, stat = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then
        return math.floor(stat)
    end
    success, stat = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Ping"]:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then
        return math.floor(stat)
    end
    success, stat = pcall(function()
        return game:GetService("Stats").Network.Ping:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then
        return math.floor(stat)
    end
    local sent = tick()
    local dummy = Instance.new("Part")
    dummy.Parent = workspace
    local received = tick()
    dummy:Destroy()
    local estimate = math.floor((received - sent) * 500)
    if estimate > 0 then return estimate end
    return 0
end

task.wait(1)
WindUI:Notify({
    Title = "Executor Detected",
    Content = "You are using: " .. executorName,
    Duration = 5,
    Icon = "terminal"
})

-- ================== COPY TO CLIPBOARD ==================
local function copyToClipboard(text)
    if setclipboard then
        setclipboard(text)
    elseif syn and syn.write_clipboard then
        syn.write_clipboard(text)
    elseif toclipboard then
        toclipboard(text)
    else
        pcall(function()
            local input = Instance.new("TextBox")
            input.Text = text
            input.Parent = game:GetService("CoreGui")
            input:CaptureFocus()
            input:SelectAll()
            task.wait(0.1)
            input.Parent = nil
        end)
    end
end

-- ================== THEME ==================
WindUI:AddTheme({
    Name = "ZyroTheme",
    Font = Font.fromName("GothamSSm", Enum.FontWeight.Medium),
    Background = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#1a1a1a") },
        ["100"] = { Color = Color3.fromHex("#1a1a1a") }
    }, { Rotation = 180 }),
    Accent = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#2563eb") },
        ["100"] = { Color = Color3.fromHex("#1d4ed8") }
    }, { Rotation = 90 }),
    SideBar = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#141414") },
        ["100"] = { Color = Color3.fromHex("#141414") }
    }, { Rotation = 180 }),
    Tab = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#222222") },
        ["100"] = { Color = Color3.fromHex("#222222") }
    }, { Rotation = 90 }),
    Toggle = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#16a34a") },
        ["100"] = { Color = Color3.fromHex("#15803d") }
    }, { Rotation = 90 }),
    Button = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#2563eb") },
        ["100"] = { Color = Color3.fromHex("#1e40af") }
    }, { Rotation = 135 }),
    Text = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#ffffff") },
        ["100"] = { Color = Color3.fromHex("#d1d5db") }
    }, { Rotation = 90 }),
    Icon = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#ffffff") },
        ["100"] = { Color = Color3.fromHex("#9ca3af") }
    }, { Rotation = 90 }),
})

-- ================== LOADING SCREEN ==================
local function showLoadingScreen()
    local blur = Instance.new("BlurEffect", game:GetService("Lighting"))
    blur.Size = 0
    TweenService:Create(blur, TweenInfo.new(0.6), { Size = 18 }):Play()

    local screenGui = Instance.new("ScreenGui")
    screenGui.Parent = player:WaitForChild("PlayerGui")
    screenGui.Name = "ZyroYonkIntro"
    screenGui.ResetOnSpawn = false
    screenGui.IgnoreGuiInset = true

    local frame = Instance.new("Frame", screenGui)
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1

    local bg = Instance.new("Frame", frame)
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(25, 0, 40)
    bg.BackgroundTransparency = 1
    TweenService:Create(bg, TweenInfo.new(0.8), { BackgroundTransparency = 0.2 }):Play()

    local word = "StarForge Hub"
    local letters = {}

    for i = 1, #word do
        local char = word:sub(i, i)
        local label = Instance.new("TextLabel")
        label.Text = char
        label.Font = Enum.Font.GothamBlack
        label.TextColor3 = Color3.new(1, 1, 1)
        label.TextTransparency = 1
        label.TextSize = 20
        label.Size = UDim2.new(0, 60, 0, 60)
        label.AnchorPoint = Vector2.new(0.5, 0.5)
        label.Position = UDim2.new(0.5, (i - (#word / 2 + 0.5)) * 55, 0.5, 20)
        label.BackgroundTransparency = 1
        label.Parent = frame

        local gradient = Instance.new("UIGradient")
        gradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(200, 120, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 0, 200))
        })
        gradient.Rotation = 90
        gradient.Parent = label

        TweenService:Create(label, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            TextTransparency = 0,
            TextSize = 55,
            Position = UDim2.new(0.5, (i - (#word / 2 + 0.5)) * 55, 0.5, 0)
        }):Play()

        table.insert(letters, label)
        task.wait(0.12)
    end

    task.wait(2)

    for _, label in pairs(letters) do
        TweenService:Create(label, TweenInfo.new(0.4), { TextTransparency = 1, TextSize = 20 }):Play()
    end

    TweenService:Create(bg, TweenInfo.new(0.6), { BackgroundTransparency = 1 }):Play()
    TweenService:Create(blur, TweenInfo.new(0.6), { Size = 0 }):Play()
    task.wait(0.7)
    screenGui:Destroy()
    blur:Destroy()
end

-- ================== MAIN GUI (WITH KEY SYSTEM) ==================
local function showGUI()
    local coolTitle = "Find and snipe Brainrot【update】"
    local profileIconUrl = "https://i.pinimg.com/736x/37/3a/b3/373ab3e072178f47dcb7fdafaddb6854.jpg"

    local Window = WindUI:CreateWindow({
        Title = coolTitle,
        Icon = "swords",
        Author = "By vlood" .. utf8.char(0xE000),
        Folder = "ZyroYonk Hub",
        Size = UDim2.fromOffset(560, 380),
        MinSize = Vector2.new(560, 350),
        MaxSize = Vector2.new(850, 560),
        Transparent = false,
        Theme = "ZyroTheme",
        Resizable = true,
        SideBarWidth = 200,
        HideSearchBar = false,
        ScrollBarEnabled = false,
        User = { Enabled = true, Anonymous = false, Callback = function() end },
                KeySystem = {                                                   
            Note = "Enter your PlatoBoost key to access StarForge Hub.",              
            API = {                                                     
                { -- PlatoBoost
                    Type = "platoboost",                                
                    ServiceId = 22001, -- Your PlatoBoost service ID
                    Secret = "945af45d-5779-4a05-a31c-432930563763", 
                },                                                      
            },                                                          
        }
    })

    -- Tags: version + live ping
    local versionTag = Window:Tag({
        Title = "v1.0.1",
        Icon = "tag",
        Color = Color3.fromHex("#2563eb"),
        Radius = 100
    })

    -- Optional: update ping every 5 seconds
    task.spawn(function()
        while true do
            local ping = getPing()
            if versionTag then
                versionTag:SetTitle("v1.0.2 | " .. ping .. "ms")
            end
            task.wait(5)
        end
    end)

    Window:EditOpenButton({
        Title = "Open ZyroYonk Hub",
        Icon = "swords",
        CornerRadius = UDim.new(0, 8),
        StrokeThickness = 1,
        Color = ColorSequence.new(Color3.fromHex("#2563eb"), Color3.fromHex("#1d4ed8")),
        OnlyMobile = false,
        Enabled = true,
        Draggable = true,
    })

    -- ========== CHANGELOG TAB ==========
    local ChangelogTab = Window:Tab({ Title = "Changelog", Icon = "scroll" })
    
    -- Account Information Section
    local accountAge = player.AccountAge .. " days"
    ChangelogTab:Paragraph({
        Title = "Hello, " .. player.DisplayName,
        Desc = "Executor: " .. executorName .. "\nAccount Age: " .. accountAge .. "\nUserID: " .. player.UserId .. "\nStatus: " .. (player.MembershipType == Enum.MembershipType.Premium and "Premium" or "Normal"),
        Color = Color3.fromHex("#222222"),
        Thumbnail = profileIconUrl,
        ThumbnailSize = 100,
    })
    
    ChangelogTab:Paragraph({
        Title = "Initial Release v1.0.0",
        Desc = "- First public release\n- Auto Shoot feature\n- Auto Upgrade Attack\n- Auto Sell All\n- Auto Rebirth\n- Auto Buy Plot\n- Auto Buy Gun (1-14)\n- Fly & Movement Speed\n- Discord Support",
        Color = Color3.fromHex("#222222"),
        Thumbnail = profileIconUrl,
        ThumbnailSize = 100,
    })

    -- ========== MAIN FEATURES TAB ==========
    local MainTab = Window:Tab({ Title = "Main Features", Icon = "house" })

    -- Auto Shoot Toggle
    local autoShootEnabled = false
    local autoShootThread = nil
    local shootCount = 0

    local shootData = {
        {{-471.6983947753906, 285.75494384765625, -2016.27490234375}, {0.3455265462398529, -0.3089560866355896, -0.8860911130905151}},
        {{-470.85443115234375, 285.7103271484375, -2015.6190185546875}, {0.5549849271774292, -0.38052788376808167, -0.7397230863571167}},
        {{-470.1102600097656, 285.75128173828125, -2014.3397216796875}, {0.7849065661430359, -0.4229290187358856, -0.4528273940086365}},
        {{-469.8780822753906, 285.7279357910156, -2013.461181640625}, {0.7475839257240295, -0.6313775181770325, -0.20610855519771576}},
        {{-470.8885803222656, 285.7431335449219, -1993.533935546875}, {0.6723112463951111, -0.6342977285385132, 0.381659597158432}},
        {{-471.6382141113281, 285.73736572265625, -2003.823486328125}, {0.8077524304389954, -0.39389580488204956, 0.4386139214038849}},
        {{-472.1693420410156, 285.7237243652344, -1932.02099609375}, {0.9413658380508423, -0.29236742854118347, 0.16837958991527557}},
        {{-471.83026123046875, 285.7507019042969, -1911.093017578125}, {0.6360939741134644, -0.2901504337787628, 0.7149806618690491}},
        {{-471.3382263183594, 285.75677490234375, -1905.873291015625}, {0.7058397531509399, -0.4485594630241394, 0.5482559204101562}},
        {{-470.117919921875, 285.7376403808594, -1897.9547119140625}, {0.9449105262756348, -0.1812872588634491, 0.27254199981689453}},
        {{-470.1703796386719, 285.7396545410156, -1899.4449462890625}, {0.8948274850845337, -0.3528699576854706, 0.2734347879886627}},
        {{-469.8833312988281, 285.7412109375, -1900.16455078125}, {0.9723716974258423, -0.21581357717514038, 0.0889820083975792}},
        {{-470.81597900390625, 285.75579833984375, -1923.5155029296875}, {0.9722030758857727, -0.23103779554367065, -0.03798280656337738}},
        {{-476.8699035644531, 285.7528991699219, -1941.9061279296875}, {0.9109014272689819, -0.2338288426399231, -0.33997464179992676}},
        {{-476.7995300292969, 285.72900390625, -1941.41943359375}, {0.9570794701576233, -0.1799549013376236, -0.22718952596187592}},
        {{-485.32000732421875, 285.7373962402344, -1965.66748046875}, {0.9456611275672913, -0.13352110981941223, -0.2964744567871094}},
        {{-486.58203125, 285.7404479980469, -1988.0718994140625}, {0.9630671739578247, -0.09645586460828781, -0.2513919770717621}},
        {{-487.13800048828125, 285.7420349121094, -1998.0526123046875}, {0.9243308901786804, -0.12477518618106842, -0.36061567068099976}},
        {{-487.2914733886719, 285.7338562011719, -1998.6295166015625}, {0.8564808964729309, -0.13282166421413422, -0.4987974166870117}},
        {{-487.35333251953125, 285.7487487792969, -1998.76708984375}, {0.8327987194061279, -0.16918525099754333, -0.5270888209342957}},
        {{-469.9605712890625, 285.7704772949219, -1849.5008544921875}, {0.9602226614952087, -0.1335553228855133, -0.24522525072097778}},
        {{-470.31353759765625, 285.75689697265625, -1847.6077880859375}, {0.9575721025466919, -0.13016995787620544, 0.25712156295776367}},
        {{-478.6304626464844, 285.70745849609375, -1853.2899169921875}, {0.9579479694366455, -0.17240726947784424, 0.2293715924024582}},
    }

    local shootIndex = 1

    MainTab:Toggle({
        Title = "Auto Shoot",
        Desc = "Loops through all shoot positions as fast as possible (auto reloads after each cycle)",
        Default = false,
        Callback = function(state)
            autoShootEnabled = state
            if state then
                shootCount = 0
                shootIndex = 1
                WindUI:Notify({
                    Title = "Auto Shoot ON",
                    Content = "Shooting loop started (MAX SPEED with auto reload)",
                    Duration = 3,
                    Icon = "target"
                })
                autoShootThread = task.spawn(function()
                    while autoShootEnabled do
                        pcall(function()
                            local data = shootData[shootIndex]
                            if data then
                                local pos = data[1]
                                local dir = data[2]
                                local posVector = Vector3.new(pos[1], pos[2], pos[3])
                                local dirVector = Vector3.new(dir[1], dir[2], dir[3])
                                local shootEvent = ReplicatedStorage:WaitForChild("Remote"):WaitForChild("ShootEvent")
                                shootEvent:FireServer(posVector, dirVector)
                            end
                            
                            shootIndex = shootIndex + 1
                            
                            if shootIndex > #shootData then
                                shootIndex = 1
                                shootCount = shootCount + 1
                                local reloadEvent = ReplicatedStorage:WaitForChild("Remote"):WaitForChild("ReloadEvent")
                                reloadEvent:FireServer("Reload")
                            end
                        end)
                        task.wait(0)
                    end
                end)
            else
                if autoShootThread then
                    task.cancel(autoShootThread)
                    autoShootThread = nil
                end
                shootIndex = 1
                shootCount = 0
                WindUI:Notify({
                    Title = "Auto Shoot OFF",
                    Content = "Shooting loop stopped",
                    Duration = 2,
                    Icon = "target-off"
                })
            end
        end
    })

    MainTab:Section({ Title = "Upgrades" })

    -- Auto Upgrade Attack Toggle
    local autoUpgradeAttackEnabled = false
    local autoUpgradeAttackThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade Attack",
        Desc = "Automatically upgrades your attack",
        Default = false,
        Callback = function(state)
            autoUpgradeAttackEnabled = state
            if state then
                WindUI:Notify({
                    Title = "Auto Upgrade Attack ON",
                    Content = "Upgrading attack...",
                    Duration = 3,
                    Icon = "trending-up"
                })
                autoUpgradeAttackThread = task.spawn(function()
                    while autoUpgradeAttackEnabled do
                        pcall(function()
                            local args = {"UpgradeAttack", 1}
                            game:GetService("ReplicatedStorage"):WaitForChild("Remote"):WaitForChild("Attack"):FireServer(unpack(args))
                        end)
                        task.wait(0.1)
                    end
                end)
            else
                if autoUpgradeAttackThread then
                    task.cancel(autoUpgradeAttackThread)
                    autoUpgradeAttackThread = nil
                end
                WindUI:Notify({
                    Title = "Auto Upgrade Attack OFF",
                    Content = "Stopped upgrading attack",
                    Duration = 2,
                    Icon = "trending-down"
                })
            end
        end
    })

    -- Auto Sell All Toggle
    local autoSellAllEnabled = false
    local autoSellAllThread = nil

    MainTab:Toggle({
        Title = "Auto Sell All",
        Desc = "Automatically sells all items",
        Default = false,
        Callback = function(state)
            autoSellAllEnabled = state
            if state then
                WindUI:Notify({
                    Title = "Auto Sell All ON",
                    Content = "Selling all items...",
                    Duration = 3,
                    Icon = "shopping-cart"
                })
                autoSellAllThread = task.spawn(function()
                    while autoSellAllEnabled do
                        pcall(function()
                            local args = {"SellAll"}
                            game:GetService("ReplicatedStorage"):WaitForChild("Remote"):WaitForChild("SellF"):InvokeServer(unpack(args))
                        end)
                        task.wait(1)
                    end
                end)
            else
                if autoSellAllThread then
                    task.cancel(autoSellAllThread)
                    autoSellAllThread = nil
                end
                WindUI:Notify({
                    Title = "Auto Sell All OFF",
                    Content = "Stopped selling items",
                    Duration = 2,
                    Icon = "shopping-cart-off"
                })
            end
        end
    })

    -- Auto Rebirth Toggle
    local autoRebirthEnabled = false
    local autoRebirthThread = nil

    MainTab:Toggle({
        Title = "Auto Rebirth",
        Desc = "Automatically performs rebirth",
        Default = false,
        Callback = function(state)
            autoRebirthEnabled = state
            if state then
                WindUI:Notify({
                    Title = "Auto Rebirth ON",
                    Content = "Rebirthing...",
                    Duration = 3,
                    Icon = "refresh-cw"
                })
                autoRebirthThread = task.spawn(function()
                    while autoRebirthEnabled do
                        pcall(function()
                            local args = {"Rebirth"}
                            game:GetService("ReplicatedStorage"):WaitForChild("Remote"):WaitForChild("Rebirth"):FireServer(unpack(args))
                        end)
                        task.wait(2)
                    end
                end)
            else
                if autoRebirthThread then
                    task.cancel(autoRebirthThread)
                    autoRebirthThread = nil
                end
                WindUI:Notify({
                    Title = "Auto Rebirth OFF",
                    Content = "Stopped rebirthing",
                    Duration = 2,
                    Icon = "refresh-cw-off"
                })
            end
        end
    })

    -- Auto Buy Plot Toggle
    local autoBuyPlotEnabled = false
    local autoBuyPlotThread = nil

    MainTab:Toggle({
        Title = "Auto Buy Plot",
        Desc = "Automatically buys/upgrades floor",
        Default = false,
        Callback = function(state)
            autoBuyPlotEnabled = state
            if state then
                WindUI:Notify({
                    Title = "Auto Buy Plot ON",
                    Content = "Buying/Upgrading floor...",
                    Duration = 3,
                    Icon = "map"
                })
                autoBuyPlotThread = task.spawn(function()
                    while autoBuyPlotEnabled do
                        pcall(function()
                            local args = {"UpgradeFloor"}
                            game:GetService("ReplicatedStorage"):WaitForChild("Remote"):WaitForChild("Floor"):FireServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoBuyPlotThread then
                    task.cancel(autoBuyPlotThread)
                    autoBuyPlotThread = nil
                end
                WindUI:Notify({
                    Title = "Auto Buy Plot OFF",
                    Content = "Stopped buying/upgrading floor",
                    Duration = 2,
                    Icon = "map-off"
                })
            end
        end
    })

    MainTab:Section({ Title = "Auto Buy Gun (1-14)" })

    -- Auto Buy Gun Toggle (1 to 14)
    local autoBuyGunEnabled = false
    local autoBuyGunThread = nil
    local currentGunLevel = 1

    MainTab:Toggle({
        Title = "Auto Buy Gun",
        Desc = "Automatically buys guns from level 1 to 14 (cycles through)",
        Default = false,
        Callback = function(state)
            autoBuyGunEnabled = state
            if state then
                currentGunLevel = 1
                WindUI:Notify({
                    Title = "Auto Buy Gun ON",
                    Content = "Buying guns 1-14 in a loop...",
                    Duration = 3,
                    Icon = "crosshair"
                })
                autoBuyGunThread = task.spawn(function()
                    while autoBuyGunEnabled do
                        pcall(function()
                            local args = {"BuyWeapon", currentGunLevel}
                            game:GetService("ReplicatedStorage"):WaitForChild("Remote"):WaitForChild("Weapon"):FireServer(unpack(args))
                            
                            -- Move to next gun level
                            currentGunLevel = currentGunLevel + 1
                            if currentGunLevel > 14 then
                                currentGunLevel = 1
                            end
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoBuyGunThread then
                    task.cancel(autoBuyGunThread)
                    autoBuyGunThread = nil
                end
                currentGunLevel = 1
                WindUI:Notify({
                    Title = "Auto Buy Gun OFF",
                    Content = "Stopped buying guns",
                    Duration = 2,
                    Icon = "crosshair-off"
                })
            end
        end
    })

    -- ========== MISC TAB ==========
    local MiscTab = Window:Tab({ Title = "Misc", Icon = "wrench" })

    -- Fly Section
    MiscTab:Section({ Title = "Fly" })

    local flySpeed = 50
    local flyEnabled = false
    local flyBodyVelocity = nil
    local flyBodyGyro = nil
    local flyLoopThread = nil

    local function startFly()
        local char = player.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChild("Humanoid")
        if not root or not hum then return end

        hum.PlatformStand = true

        flyBodyVelocity = Instance.new("BodyVelocity")
        flyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
        flyBodyVelocity.MaxForce = Vector3.new(1e9, 1e9, 1e9)
        flyBodyVelocity.Parent = root

        flyBodyGyro = Instance.new("BodyGyro")
        flyBodyGyro.MaxTorque = Vector3.new(1e9, 1e9, 1e9)
        flyBodyGyro.D = 100
        flyBodyGyro.CFrame = root.CFrame
        flyBodyGyro.Parent = root

        flyLoopThread = task.spawn(function()
            while flyEnabled do
                local char = player.Character
                if not char then break end
                local root = char:FindFirstChild("HumanoidRootPart")
                if not root then break end
                local camera = workspace.CurrentCamera
                local dir = Vector3.new()

                if UserInputService:IsKeyDown(Enum.KeyCode.W) then
                    dir = dir + camera.CFrame.LookVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then
                    dir = dir - camera.CFrame.LookVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then
                    dir = dir - camera.CFrame.RightVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then
                    dir = dir + camera.CFrame.RightVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
                    dir = dir + Vector3.new(0, 1, 0)
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) or
                   UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
                    dir = dir - Vector3.new(0, 1, 0)
                end

                if dir.Magnitude > 0 then
                    flyBodyVelocity.Velocity = dir.Unit * flySpeed
                else
                    flyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
                end

                flyBodyGyro.CFrame = camera.CFrame
                task.wait()
            end
        end)
    end

    local function stopFly()
        if flyBodyVelocity then flyBodyVelocity:Destroy() flyBodyVelocity = nil end
        if flyBodyGyro then flyBodyGyro:Destroy() flyBodyGyro = nil end
        if flyLoopThread then task.cancel(flyLoopThread) flyLoopThread = nil end

        local char = player.Character
        if char then
            local hum = char:FindFirstChild("Humanoid")
            if hum then hum.PlatformStand = false end
        end
    end

    MiscTab:Input({
        Title = "Fly Speed",
        Desc = "Set fly speed (default: 50)",
        Placeholder = "Enter speed...",
        Numeric = true,
        Finished = true,
        Callback = function(value)
            local num = tonumber(value)
            if num and num > 0 then
                flySpeed = num
                WindUI:Notify({
                    Title = "Fly Speed Set",
                    Content = "Speed: " .. tostring(num),
                    Duration = 2,
                    Icon = "zap"
                })
            end
        end
    })

    MiscTab:Toggle({
        Title = "Enable Fly",
        Desc = "WASD + Space / LCtrl",
        Default = false,
        Callback = function(state)
            flyEnabled = state
            if state then
                startFly()
                WindUI:Notify({
                    Title = "Fly ON",
                    Content = "Speed: " .. tostring(flySpeed),
                    Duration = 3,
                    Icon = "arrow-up"
                })
            else
                stopFly()
                WindUI:Notify({
                    Title = "Fly OFF",
                    Content = "Fly disabled.",
                    Duration = 2,
                    Icon = "arrow-down"
                })
            end
        end
    })

    player.CharacterAdded:Connect(function()
        if flyEnabled then
            task.wait(1)
            startFly()
        end
    end)

    -- Movement Speed Section
    MiscTab:Section({ Title = "Movement Speed" })

    local moveSpeed = 16
    local moveSpeedEnabled = false
    local moveSpeedConn = nil

    MiscTab:Input({
        Title = "Movement Speed",
        Desc = "Set walk speed (default: 16)",
        Placeholder = "Enter speed...",
        Numeric = true,
        Finished = true,
        Callback = function(value)
            local num = tonumber(value)
            if num and num > 0 then
                moveSpeed = num
                if moveSpeedEnabled then
                    local char = player.Character
                    if char then
                        local hum = char:FindFirstChild("Humanoid")
                        if hum then hum.WalkSpeed = moveSpeed end
                    end
                end
                WindUI:Notify({
                    Title = "Move Speed Set",
                    Content = "Speed: " .. tostring(num),
                    Duration = 2,
                    Icon = "zap"
                })
            end
        end
    })

    MiscTab:Toggle({
        Title = "Enable Move Speed",
        Default = false,
        Callback = function(state)
            moveSpeedEnabled = state

            local function applySpeed()
                local char = player.Character
                if char then
                    local hum = char:FindFirstChild("Humanoid")
                    if hum then
                        hum.WalkSpeed = state and moveSpeed or 16
                    end
                end
            end

            applySpeed()

            if state then
                moveSpeedConn = player.CharacterAdded:Connect(function(char)
                    task.wait(1)
                    if moveSpeedEnabled then
                        local hum = char:WaitForChild("Humanoid")
                        hum.WalkSpeed = moveSpeed
                    end
                end)
                WindUI:Notify({
                    Title = "Move Speed ON",
                    Content = "Speed: " .. tostring(moveSpeed),
                    Duration = 3,
                    Icon = "activity"
                })
            else
                if moveSpeedConn then
                    moveSpeedConn:Disconnect()
                    moveSpeedConn = nil
                end
                WindUI:Notify({
                    Title = "Move Speed OFF",
                    Content = "Reset to 16.",
                    Duration = 2,
                    Icon = "minus-circle"
                })
            end
        end
    })

    -- Reset Button
    MiscTab:Section({ Title = "Reset" })
    MiscTab:Button({
        Title = "Reset Position & Health",
        Icon = "refresh-cw",
        Callback = function()
            local char = player.Character
            if not char then
                WindUI:Notify({ Title = "Error", Content = "Character not found.", Duration = 2, Icon = "alert-triangle" })
                return
            end
            local hum = char:FindFirstChild("Humanoid")
            local root = char:FindFirstChild("HumanoidRootPart")
            if not hum or not root then
                WindUI:Notify({ Title = "Error", Content = "Humanoid or RootPart missing.", Duration = 2, Icon = "alert-triangle" })
                return
            end

            hum.Health = hum.MaxHealth

            local destination = workspace.Break_Map and workspace.Break_Map.Locations and workspace.Break_Map.Locations.CommonFloorBest
            if destination then
                root.CFrame = destination.CFrame + Vector3.new(0, 5, 0)
                WindUI:Notify({ Title = "Reset", Content = "Teleported to Common Floor & fully healed.", Duration = 3, Icon = "check-circle" })
            else
                WindUI:Notify({ Title = "Warning", Content = "CommonFloorBest not found, only healed.", Duration = 3, Icon = "alert-triangle" })
            end
        end
    })

    -- ========== SUPPORT TAB ==========
    local SupportTab = Window:Tab({ Title = "Support", Icon = "heart" })

    local InviteCode = "D5NKtBJEzz"
    local DiscordAPI = "https://discord.com/api/v10/invites/" .. InviteCode .. "?with_counts=true&with_expiration=true"

    local function httpGet(url)
        local body, success = nil, false
        if syn and syn.request then
            success, body = pcall(function() return syn.request({ Url = url, Method = "GET" }).Body end)
        elseif http and http.request then
            success, body = pcall(function() return http.request({ Url = url, Method = "GET" }).Body end)
        elseif request then
            success, body = pcall(function() return request({ Url = url, Method = "GET" }).Body end)
        elseif http_request then
            success, body = pcall(function() return http_request({ Url = url, Method = "GET" }).Body end)
        end
        if success and body then return body else return nil end
    end

    local Response, raw = {}, httpGet(DiscordAPI)
    if raw then pcall(function() Response = HttpService:JSONDecode(raw) end) end

    local onlineCount = Response and Response.approximate_presence_count or "?"
    local totalCount = Response and Response.approximate_member_count or "?"
    local guildName = (Response and Response.guild and Response.guild.name) or "ZyroYonk Hub"
    local iconUrl = nil
    if Response and Response.guild and Response.guild.id and Response.guild.icon then
        iconUrl = "https://cdn.discordapp.com/icons/"
            .. tostring(Response.guild.id) .. "/"
            .. tostring(Response.guild.icon) .. ".png?size=256"
    end

    SupportTab:Section({ Title = "Discord Server" })

    local cardProps = {
        Title = guildName,
        Desc = "Online: " .. tostring(onlineCount) .. "   Members: " .. tostring(totalCount) .. "\n\nJoin for updates & support.",
        Color = Color3.fromHex("#222222"),
        ImageSize = 64,
        ThumbnailSize = 100,
        Buttons = {
            {
                Title = "Copy Invite",
                Callback = function()
                    copyToClipboard("https://discord.gg/" .. InviteCode)
                    WindUI:Notify({ Title = "Copied!", Content = "Invite link copied.", Duration = 2, Icon = "clipboard-check" })
                end
            },
            {
                Title = "Join Discord",
                Callback = function()
                    if openUrl then
                        openUrl("https://discord.gg/" .. InviteCode)
                    else
                        copyToClipboard("https://discord.gg/" .. InviteCode)
                        WindUI:Notify({ Title = "Link Copied", Content = "Open in your browser.", Duration = 3, Icon = "globe" })
                    end
                end
            }
        }
    }
    if iconUrl then cardProps.Image = iconUrl end
    SupportTab:Paragraph(cardProps)

    SupportTab:Section({ Title = "Help" })

    SupportTab:Paragraph({
        Title = "Executor",
        Desc = "Using: " .. executorName .. "\nReport issues in Discord.",
        Color = Color3.fromHex("#222222"),
    })

    SupportTab:Button({
        Title = "Report a Bug",
        Icon = "bug",
        Callback = function()
            copyToClipboard("Bug Report\nExecutor: " .. executorName .. "\nScript: ZyroYonk Hub v1.0.2\n\nDescribe the issue:")
            WindUI:Notify({ Title = "Template Copied", Content = "Paste in Discord support channel.", Duration = 4, Icon = "message-square" })
        end
    })
end

-- ================== STARTUP ==================
showLoadingScreen()
task.wait(2.5)
showGUI()
