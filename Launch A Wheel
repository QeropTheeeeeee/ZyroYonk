local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local MarketplaceService = game:GetService("MarketplaceService")
local player = Players.LocalPlayer

-- ===== Executor Detection =====
local function getExecutorName()
    if identifyexecutor then
        local success, name = pcall(identifyexecutor)
        if success and name and name ~= "" then return name end
    end
    if getexecutorname then
        local success, name = pcall(getexecutorname)
        if success and name and name ~= "" then return name end
    end
    if syn and syn.crypt and syn.crypt.custom then return "Synapse" end
    if krnl then return "Krnl" end
    if fluxus then return "Fluxus" end
    if electron then return "Electron" end
    if is_sirhurt_closure then return "SirHurt" end
    if getgenv and getgenv().IS_SCRIPT_WARE then return "ScriptWare" end
    if getgenv and getgenv().IS_VEGA_X then return "Vega X" end
    if getgenv and getgenv().IS_DELTA then return "Delta" end
    if getgenv and getgenv().IS_CODE_X then return "Code X" end
    return "Unknown"
end
local executorName = getExecutorName()

-- ===== Ping =====
local function getPing()
    local success, stat = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then return math.floor(stat) end
    success, stat = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Ping"]:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then return math.floor(stat) end
    success, stat = pcall(function()
        return game:GetService("Stats").Network.Ping:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then return math.floor(stat) end
    return 0
end

task.wait(1)
WindUI:Notify({
    Title = "Executor Detected",
    Content = "Using: " .. executorName,
    Duration = 5,
    Icon = "terminal"
})

-- ===== Copy =====
local function copyToClipboard(text)
    if setclipboard then setclipboard(text)
    elseif syn and syn.write_clipboard then syn.write_clipboard(text)
    elseif toclipboard then toclipboard(text)
    else
        pcall(function()
            local input = Instance.new("TextBox")
            input.Text = text
            input.Parent = game:GetService("CoreGui")
            input:CaptureFocus()
            input:SelectAll()
            task.wait(0.1)
            input.Parent = nil
        end)
    end
end

-- ===== HWID =====
local function getHWID()
    local success, id = pcall(function()
        return game:GetService("RbxAnalyticsService"):GetClientId()
    end)
    if success and id and id ~= "" then return id end
    success, id = pcall(function()
        return game:GetService("HttpService"):GenerateGUID(false)
    end)
    if success and id then return id end
    return "Unknown"
end
local hwid = getHWID()

-- ===== HTTP GET (only for Discord invite info, keep as is) =====
local function httpGet(url)
    if syn and syn.request then
        local res = syn.request({ Url = url, Method = "GET" })
        if res and res.Body then return res.Body end
    elseif request then
        local res = request({ Url = url, Method = "GET" })
        if res and res.Body then return res.Body end
    elseif http_request then
        local res = http_request({ Url = url, Method = "GET" })
        if res and res.Body then return res.Body end
    end
    return nil
end

-- ===== Theme =====
WindUI:AddTheme({
    Name = "ZyroTheme",
    Font = Font.fromName("GothamSSm", Enum.FontWeight.Medium),
    Background = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#1a1a1a") }, ["100"] = { Color = Color3.fromHex("#1a1a1a") } }, { Rotation = 180 }),
    Accent = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#2563eb") }, ["100"] = { Color = Color3.fromHex("#1d4ed8") } }, { Rotation = 90 }),
    SideBar = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#141414") }, ["100"] = { Color = Color3.fromHex("#141414") } }, { Rotation = 180 }),
    Tab = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#222222") }, ["100"] = { Color = Color3.fromHex("#222222") } }, { Rotation = 90 }),
    Toggle = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#16a34a") }, ["100"] = { Color = Color3.fromHex("#15803d") } }, { Rotation = 90 }),
    Button = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#2563eb") }, ["100"] = { Color = Color3.fromHex("#1e40af") } }, { Rotation = 135 }),
    Text = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#ffffff") }, ["100"] = { Color = Color3.fromHex("#d1d5db") } }, { Rotation = 90 }),
    Icon = WindUI:Gradient({ ["0"] = { Color = Color3.fromHex("#ffffff") }, ["100"] = { Color = Color3.fromHex("#9ca3af") } }, { Rotation = 90 }),
})

-- ===== Anti-AFK =====
local antiAfkRunning = false
local antiAfkThread = nil
local function antiAfkLoop()
    while antiAfkRunning do
        local success, vu = pcall(function() return game:GetService("VirtualUser") end)
        if success and vu then
            pcall(function() vu:CaptureController() vu:ClickButton2(Vector2.new()) end)
        else
            local char = player.Character
            if char then
                local hrp = char:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local original = hrp.CFrame
                    hrp.CFrame = original + Vector3.new(0, 0.01, 0)
                    task.wait(0.05)
                    hrp.CFrame = original
                end
            end
        end
        task.wait(30)
    end
end

-- ===== Loading Screen =====
local function showLoadingScreen()
    local blur = Instance.new("BlurEffect", game:GetService("Lighting"))
    blur.Size = 0
    TweenService:Create(blur, TweenInfo.new(0.6), { Size = 18 }):Play()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Parent = player:WaitForChild("PlayerGui")
    screenGui.Name = "ZyroYonkIntro"
    screenGui.ResetOnSpawn = false
    screenGui.IgnoreGuiInset = true
    local frame = Instance.new("Frame", screenGui)
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1
    local bg = Instance.new("Frame", frame)
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(25, 0, 40)
    bg.BackgroundTransparency = 1
    TweenService:Create(bg, TweenInfo.new(0.8), { BackgroundTransparency = 0.2 }):Play()
    local word = "StarForge Hub"
    local letters = {}
    for i = 1, #word do
        local char = word:sub(i, i)
        local label = Instance.new("TextLabel")
        label.Text = char
        label.Font = Enum.Font.GothamBlack
        label.TextColor3 = Color3.new(1, 1, 1)
        label.TextTransparency = 1
        label.TextSize = 20
        label.Size = UDim2.new(0, 60, 0, 60)
        label.AnchorPoint = Vector2.new(0.5, 0.5)
        label.Position = UDim2.new(0.5, (i - (#word / 2 + 0.5)) * 55, 0.5, 20)
        label.BackgroundTransparency = 1
        label.Parent = frame
        local gradient = Instance.new("UIGradient")
        gradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(200, 120, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 0, 200))
        })
        gradient.Rotation = 90
        gradient.Parent = label
        TweenService:Create(label, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            TextTransparency = 0,
            TextSize = 55,
            Position = UDim2.new(0.5, (i - (#word / 2 + 0.5)) * 55, 0.5, 0)
        }):Play()
        table.insert(letters, label)
        task.wait(0.12)
    end
    task.wait(2)
    for _, label in pairs(letters) do
        TweenService:Create(label, TweenInfo.new(0.4), { TextTransparency = 1, TextSize = 20 }):Play()
    end
    TweenService:Create(bg, TweenInfo.new(0.6), { BackgroundTransparency = 1 }):Play()
    TweenService:Create(blur, TweenInfo.new(0.6), { Size = 0 }):Play()
    task.wait(0.7)
    screenGui:Destroy()
    blur:Destroy()
end

-- ===== Main GUI =====
local function showGUI()
    local Window = WindUI:CreateWindow({
        Title = "Launch A Wheel",
        Icon = "hammer",
        Author = "By Vloods" .. utf8.char(0xE000),
        Folder = "ZyroYonk Hub",
        Size = UDim2.fromOffset(560, 380),
        MinSize = Vector2.new(560, 350),
        MaxSize = Vector2.new(850, 560),
        Theme = "ZyroTheme",
        Resizable = true,
        SideBarWidth = 200,
        HideSearchBar = false,
        ScrollBarEnabled = false,
        User = { Enabled = true, Anonymous = false, Callback = function() end },
    })

    -- ===== VERSION & PING TAGS =====
    local versionTag = Window:Tag({
        Title = "v1.0.1",
        Icon = "tag",
        Color = Color3.fromHex("#2563eb"),
        Radius = 100
    })

    local pingTag = Window:Tag({
        Title = "0ms",
        Icon = "wifi",
        Color = Color3.fromHex("#22c55e"),
        Radius = 100
    })

    -- Real Ping Updater
    task.spawn(function()
        while true do
            local ping = getPing()
            
            if pingTag and pingTag.SetTitle then
                local pingColor = (ping <= 70 and "#22c55e") or (ping <= 140 and "#eab308") or "#ef4444"
                pingTag:SetTitle(ping .. "ms")
                -- pingTag:SetColor(Color3.fromHex(pingColor)) -- Uncomment for color change
            end
            
            task.wait(0.8)
        end
    end)

    Window:EditOpenButton({
        Title = "Open Hub",
        Icon = "hammer",
        CornerRadius = UDim.new(0, 8),
        StrokeThickness = 1,
        Color = ColorSequence.new(Color3.fromHex("#2563eb"), Color3.fromHex("#1d4ed8")),
        OnlyMobile = false,
        Enabled = true,
        Draggable = true,
    })

    -- ===== INFORMATION TAB =====
    local InfoTab = Window:Tab({ Title = "Information", Icon = "info" })

    local avatarUrl = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. player.UserId .. "&width=100&height=100&format=png"
    InfoTab:Paragraph({
        Title = "👋 Hi " .. player.Name .. "!",
        Desc = "Welcome to StarForge Hub.\nIf you encounter any issues, please join our Discord via the Support tab.",
        Color = Color3.fromHex("#222222"),
        Image = avatarUrl,
        ImageSize = 64,
        ThumbnailSize = 80,
    })

    Window:Divider()

    -- ===== PLAYER SELECTOR =====
    InfoTab:Section({ Title = "Player Selector" })

    local selectedPlayerName = nil
    local playersDropdown = nil
    local profileParagraph = nil

    local function updateProfile(plr)
        if not plr then
            if profileParagraph then
                profileParagraph:SetDesc("No player selected.")
                profileParagraph:SetImage("")
            end
            return
        end

        local age = plr.AccountAge or 0
        local avatar = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. plr.UserId .. "&width=100&height=100&format=png"
        local desc = string.format("**Name:** %s\n**Account Age:** %d days\n**User ID:** %d", plr.Name, age, plr.UserId)

        if profileParagraph then
            profileParagraph:SetDesc(desc)
            profileParagraph:SetImage(avatar)
        else
            profileParagraph = InfoTab:Paragraph({
                Title = "Selected Player Profile",
                Desc = desc,
                Color = Color3.fromHex("#222222"),
                Image = avatar,
                ImageSize = 64,
                ThumbnailSize = 80,
            })
        end
    end

    local function getPlayerNames()
        local playerNames = {}
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player then
                table.insert(playerNames, p.Name)
            end
        end
        if #playerNames == 0 then
            table.insert(playerNames, "No other players")
        end
        return playerNames
    end

    local function refreshPlayersDropdown()
        local playerNames = getPlayerNames()

        if playersDropdown then
            playersDropdown:SetValues(playerNames)
            if not table.find(playerNames, selectedPlayerName) then
                selectedPlayerName = playerNames[1]
                playersDropdown:SetValue(selectedPlayerName)
            end
        else
            playersDropdown = InfoTab:Dropdown({
                Title = "Select a player",
                Desc = "Choose a player to view their profile and teleport to them",
                Values = playerNames,
                Value = playerNames[1],
                Callback = function(option)
                    selectedPlayerName = option
                    local target = Players:FindFirstChild(option)
                    updateProfile(target)
                end
            })
            selectedPlayerName = playerNames[1]
        end

        local target = Players:FindFirstChild(selectedPlayerName)
        updateProfile(target)
    end

    refreshPlayersDropdown()

    Players.PlayerAdded:Connect(refreshPlayersDropdown)
    Players.PlayerRemoving:Connect(refreshPlayersDropdown)

    InfoTab:Button({
        Title = "Refresh Players",
        Icon = "refresh-cw",
        Callback = function()
            refreshPlayersDropdown()
            WindUI:Notify({
                Title = "Players Refreshed",
                Duration = 1,
                Icon = "check"
            })
        end
    })

    InfoTab:Button({
        Title = "Teleport to Player",
        Icon = "send",
        Callback = function()
            if not selectedPlayerName or selectedPlayerName == "No other players" then
                WindUI:Notify({ Title = "No player selected", Duration = 2, Icon = "alert-circle" })
                return
            end

            local target = Players:FindFirstChild(selectedPlayerName)
            if not target then
                WindUI:Notify({ Title = "Player not found", Duration = 2, Icon = "alert-circle" })
                return
            end

            local char = target.Character
            if not char or not char:FindFirstChild("HumanoidRootPart") then
                WindUI:Notify({ Title = "Target has no character", Duration = 2, Icon = "alert-circle" })
                return
            end

            local myChar = player.Character
            if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then
                WindUI:Notify({ Title = "You have no character", Duration = 2, Icon = "alert-circle" })
                return
            end

            myChar.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(0, 2, 0)
            WindUI:Notify({ Title = "Teleported to " .. target.Name, Duration = 2, Icon = "check" })
        end
    })

    -- ===== MAIN FEATURES TAB =====
    local MainTab = Window:Tab({ Title = "Main Features", Icon = "house" })

    -- Egg Dropdown (updated with new eggs)
    local eggOptions = {
        "Basic Egg",
        "Dragon Egg",
        "Bee Egg",
        "1M Egg 🎉",
        "10M Egg🎉",
        "Desert Egg",
        "Pyramid Egg",
        "Cactus Egg",
        "Carrot Egg",
        "Turkey Egg",
        "Prickly Egg",
        "Abyss Egg",
        "Alien Egg",
        "Atlantis Egg",
        "Blaster Egg",
        "Cosmic Egg",
        "Void Egg",
        "Timeless Egg",
        "Season 1 Egg",
        "Royal Egg",
        "Money Egg",
        "Martian Egg",
        "Kraken Egg",
        "Space Egg",
        "500K Egg"
    }
    local selectedEgg = "Basic Egg"

    local eggMapping = {
        ["1M Egg 🎉"]  = "1M Egg",
        ["10M Egg🎉"]  = "10M Egg",
    }

    MainTab:Dropdown({
        Title = "Egg",
        Desc = "Select the egg to hatch",
        Values = eggOptions,
        Value = selectedEgg,
        Callback = function(option)
            selectedEgg = eggMapping[option] or option
        end
    })

    -- Hatch Type Dropdown
    local hatchOptions = {"Single", "Triple", "Octo"}
    local selectedHatch = hatchOptions[1]

    MainTab:Dropdown({
        Title = "Hatch Type",
        Desc = "Select hatch amount",
        Values = hatchOptions,
        Value = selectedHatch,
        Callback = function(option)
            selectedHatch = option
        end
    })

    -- Auto Hatch
    local autoHatchRunning = false
    local autoHatchThread = nil
    local cachedRemote = nil

    local function getHatchRemote()
        if cachedRemote then
            return cachedRemote
        end
        local success, remote = pcall(function()
            return ReplicatedStorage
                :WaitForChild("Library")
                :WaitForChild("Knit")
                :WaitForChild("Services")
                :WaitForChild("EggsService")
                :WaitForChild("RE")
                :WaitForChild("HatchEgg")
        end)
        if success then
            cachedRemote = remote
            return remote
        end
        return nil
    end

    local function autoHatchLoop()
        while autoHatchRunning do
            local remote = getHatchRemote()
            if remote then
                local args = { selectedEgg, selectedHatch }
                pcall(function()
                    remote:FireServer(unpack(args))
                end)
            else
                task.wait(1)
            end
            task.wait(0.1)
        end
    end

    MainTab:Toggle({
        Title = "Auto Hatch",
        Default = false,
        Callback = function(state)
            autoHatchRunning = state
            if state then
                if autoHatchThread then
                    task.cancel(autoHatchThread)
                end
                autoHatchThread = task.spawn(autoHatchLoop)
                WindUI:Notify({
                    Title = "Auto Hatch",
                    Content = "Started",
                    Duration = 2,
                    Icon = "egg"
                })
            else
                if autoHatchThread then
                    task.cancel(autoHatchThread)
                    autoHatchThread = nil
                end
                WindUI:Notify({
                    Title = "Auto Hatch",
                    Content = "Stopped",
                    Duration = 2,
                    Icon = "stop"
                })
            end
        end
    })

    -- Auto Rebirth
    MainTab:Section({ Title = "Auto Rebirth" })

    local rebirthDisplayValues = {"1", "3", "10", "25", "50", "100", "200", "500", "1k", "2k", "10k", "25k", "100k", "500k"}
    local rebirthIndexMap = {
        ["1"] = 1, ["3"] = 2, ["10"] = 3, ["25"] = 4,
        ["50"] = 5, ["100"] = 6, ["200"] = 7, ["500"] = 8,
        ["1k"] = 9,
        ["2k"] = 1,
        ["10k"] = 2,
        ["25k"] = 3,
        ["100k"] = 4,
        ["500k"] = 5
    }
    local selectedRebirthDisplay = "1"
    local selectedRebirthIndex = 1

    MainTab:Dropdown({
        Title = "Rebirth Amount",
        Desc = "Select how many rebirths to perform per trigger",
        Values = rebirthDisplayValues,
        Value = selectedRebirthDisplay,
        Callback = function(option)
            selectedRebirthDisplay = option
            selectedRebirthIndex = rebirthIndexMap[option] or 1
        end
    })

    local autoRebirthRunning = false
    local autoRebirthThread = nil

    local function autoRebirthLoop()
        while autoRebirthRunning do
            local args = { selectedRebirthIndex, "Upgrades" }
            pcall(function()
                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("RebirthService"):WaitForChild("RE"):WaitForChild("Rebirth")
                remote:FireServer(unpack(args))
            end)
            task.wait(0.3)
        end
    end

    MainTab:Toggle({
        Title = "Auto Rebirth",
        Default = false,
        Callback = function(state)
            autoRebirthRunning = state
            if state then
                if autoRebirthThread then task.cancel(autoRebirthThread) end
                autoRebirthThread = task.spawn(autoRebirthLoop)
                WindUI:Notify({ Title = "Auto Rebirth", Content = "Started (" .. selectedRebirthDisplay .. ")", Duration = 2, Icon = "refresh-cw" })
            else
                if autoRebirthThread then task.cancel(autoRebirthThread); autoRebirthThread = nil end
                WindUI:Notify({ Title = "Auto Rebirth", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Other Features (Auto Throw, Auto Claim Achiev, Auto Claim Reward Egg, Auto Claim Session Pass, Auto Upgrade, Auto Train Faster, Auto Buy Wheel, Auto Equip Best, Auto Claim Codes)
    MainTab:Section({ Title = "Other Features" })

    -- ====== NORMAL AUTO THROW (simple version) ======
    local autoThrowRunning = false
    local autoThrowThread = nil

    local function autoThrowLoop()
        while autoThrowRunning do
            pcall(function()
                local throwRemote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("ThrowService"):WaitForChild("RE"):WaitForChild("Throw")
                throwRemote:FireServer()
                local finishRemote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("ThrowService"):WaitForChild("RE"):WaitForChild("Finish")
                finishRemote:FireServer()
            end)
            task.wait(0.1)
        end
    end

    MainTab:Toggle({
        Title = "Auto Throw",
        Default = false,
        Callback = function(state)
            autoThrowRunning = state
            if state then
                if autoThrowThread then task.cancel(autoThrowThread) end
                autoThrowThread = task.spawn(autoThrowLoop)
                WindUI:Notify({ Title = "Auto Throw", Content = "Started", Duration = 2, Icon = "rotate-cw" })
            else
                if autoThrowThread then task.cancel(autoThrowThread); autoThrowThread = nil end
                WindUI:Notify({ Title = "Auto Throw", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Claim Achiev
    local autoClaimAchievRunning = false
    local autoClaimAchievThread = nil
    local achievTypes = {"TotalThrows", "TotalPower", "TotalCash", "TimePlayed"}
    local function autoClaimAchievLoop()
        while autoClaimAchievRunning do
            for _, achiev in ipairs(achievTypes) do
                if not autoClaimAchievRunning then break end
                local args = { achiev }
                pcall(function()
                    local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("AchievementService"):WaitForChild("RE"):WaitForChild("Claim")
                    remote:FireServer(unpack(args))
                end)
                task.wait(0.2)
            end
            task.wait(0.5)
        end
    end
    MainTab:Toggle({
        Title = "Auto Claim Achiev",
        Default = false,
        Callback = function(state)
            autoClaimAchievRunning = state
            if state then
                if autoClaimAchievThread then task.cancel(autoClaimAchievThread) end
                autoClaimAchievThread = task.spawn(autoClaimAchievLoop)
                WindUI:Notify({ Title = "Auto Claim Achiev", Content = "Started", Duration = 2, Icon = "trophy" })
            else
                if autoClaimAchievThread then task.cancel(autoClaimAchievThread); autoClaimAchievThread = nil end
                WindUI:Notify({ Title = "Auto Claim Achiev", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Claim Reward Egg
    local autoClaimRewardEggRunning = false
    local autoClaimRewardEggThread = nil
    local function autoClaimRewardEggLoop()
        while autoClaimRewardEggRunning do
            pcall(function()
                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("PlaytimeRewardService"):WaitForChild("RE"):WaitForChild("Claim")
                remote:FireServer()
            end)
            task.wait(0.5)
        end
    end
    MainTab:Toggle({
        Title = "Auto Claim Reward Egg",
        Default = false,
        Callback = function(state)
            autoClaimRewardEggRunning = state
            if state then
                if autoClaimRewardEggThread then task.cancel(autoClaimRewardEggThread) end
                autoClaimRewardEggThread = task.spawn(autoClaimRewardEggLoop)
                WindUI:Notify({ Title = "Auto Claim Reward Egg", Content = "Started", Duration = 2, Icon = "gift" })
            else
                if autoClaimRewardEggThread then task.cancel(autoClaimRewardEggThread); autoClaimRewardEggThread = nil end
                WindUI:Notify({ Title = "Auto Claim Reward Egg", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Claim Session Pass
    local autoClaimSessionPassRunning = false
    local autoClaimSessionPassThread = nil
    local function autoClaimSessionPassLoop()
        local level = 1
        while autoClaimSessionPassRunning do
            if level > 15 then level = 1 end
            local args = { "Free", level }
            pcall(function()
                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("SeasonpassService"):WaitForChild("RE"):WaitForChild("Claim")
                remote:FireServer(unpack(args))
            end)
            level = level + 1
            task.wait(0.3)
        end
    end
    MainTab:Toggle({
        Title = "Auto Claim Session Pass",
        Default = false,
        Callback = function(state)
            autoClaimSessionPassRunning = state
            if state then
                if autoClaimSessionPassThread then task.cancel(autoClaimSessionPassThread) end
                autoClaimSessionPassThread = task.spawn(autoClaimSessionPassLoop)
                WindUI:Notify({ Title = "Auto Claim Session Pass", Content = "Started", Duration = 2, Icon = "award" })
            else
                if autoClaimSessionPassThread then task.cancel(autoClaimSessionPassThread); autoClaimSessionPassThread = nil end
                WindUI:Notify({ Title = "Auto Claim Session Pass", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Upgrade
    local autoUpgradeRunning = false
    local autoUpgradeThread = nil
    local upgradeTypes = {
        "RebirthButton", "CashBoost", "PowerBoost", "PetEquip", "EggsLuck",
        "GoldChance", "HatchSpeed", "PetStorage", "TrainSpeed", "WalkSpeed"
    }

    local function autoUpgradeLoop()
        local index = 1
        while autoUpgradeRunning do
            if index > #upgradeTypes then index = 1 end
            local upgrade = upgradeTypes[index]
            local args = { upgrade }
            pcall(function()
                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradesService"):WaitForChild("RE"):WaitForChild("Upgrade")
                remote:FireServer(unpack(args))
            end)
            index = index + 1
            task.wait(0.2)
        end
    end

    MainTab:Toggle({
        Title = "Auto Upgrade",
        Default = false,
        Callback = function(state)
            autoUpgradeRunning = state
            if state then
                if autoUpgradeThread then task.cancel(autoUpgradeThread) end
                autoUpgradeThread = task.spawn(autoUpgradeLoop)
                WindUI:Notify({ Title = "Auto Upgrade", Content = "Started", Duration = 2, Icon = "arrow-up" })
            else
                if autoUpgradeThread then task.cancel(autoUpgradeThread); autoUpgradeThread = nil end
                WindUI:Notify({ Title = "Auto Upgrade", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Train Faster
    local autoTrainFasterRunning = false
    local autoTrainFasterThread = nil

    MainTab:Toggle({
        Title = "Auto Train Faster",
        Default = false,
        Callback = function(state)
            autoTrainFasterRunning = state
            if state then
                if autoTrainFasterThread then task.cancel(autoTrainFasterThread) end
                autoTrainFasterThread = task.spawn(function()
                    local remote = game:GetService("ReplicatedStorage")
                        :WaitForChild("Library")
                        :WaitForChild("Knit")
                        :WaitForChild("Services")
                        :WaitForChild("TrainingService")
                        :WaitForChild("RE")
                        :WaitForChild("Train")
                    local args = {"1|2"}
                    while autoTrainFasterRunning do
                        remote:FireServer(unpack(args))
                        task.wait(0)
                    end
                end)
                WindUI:Notify({ Title = "Auto Train Faster", Content = "Started", Duration = 2, Icon = "zap" })
            else
                if autoTrainFasterThread then task.cancel(autoTrainFasterThread); autoTrainFasterThread = nil end
                WindUI:Notify({ Title = "Auto Train Faster", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Buy Wheel
    local autoBuyWheelRunning = false
    local autoBuyWheelThread = nil
    local wheelItems = {
        "Car Wheel", "Rock Wheel", "Bike Wheel", "Mini Cooper Wheel",
        "Archery Target", "Blue Sport Wheel", "Robux Wheel", "Frisbee",
        "Toilet Paper", "Basketball Rim", "Monster Truck Wheel",
        "Rainbow Raft", "Flamingo Raft", "Shuriken",
        "Bronze Gear", "Wooden Wheel", "Silver Gear", "Barrow Wheel",
        "Thin Wooden Wheel", "No Entry Sign", "Cactus Wheel", "English Wheel",
        "Pull-Up Hoop", "Italy Wheel", "Sun", "Moon",
        "Giant Truck Wheel", "Golden Gear", "Skull",
        "Cuttingboard Wheel", "Turbine Wheel", "Broccoli Wheel", "Pot Wheel",
        "Cookie Wheel", "Corn Wheel", "Hay Bale Wheel", "Cabbage Wheel",
        "Apple Wheel", "Plate Wheel", "Tractor Steering Wheel", "Tomato Wheel",
        "Egg Wheel", "Cutter Wheel", "Farm Wheel", "Tractor Wheel"
    }

    local function autoBuyWheelLoop()
        local index = 1
        while autoBuyWheelRunning do
            if index > #wheelItems then index = 1 end
            local item = wheelItems[index]
            local args = { item }
            pcall(function()
                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("ItemsService"):WaitForChild("RE"):WaitForChild("Buy")
                remote:FireServer(unpack(args))
            end)
            index = index + 1
            task.wait(0.2)
        end
    end

    MainTab:Toggle({
        Title = "Auto Buy Wheel",
        Default = false,
        Callback = function(state)
            autoBuyWheelRunning = state
            if state then
                if autoBuyWheelThread then task.cancel(autoBuyWheelThread) end
                autoBuyWheelThread = task.spawn(autoBuyWheelLoop)
                WindUI:Notify({ Title = "Auto Buy Wheel", Content = "Started", Duration = 2, Icon = "shopping-cart" })
            else
                if autoBuyWheelThread then task.cancel(autoBuyWheelThread); autoBuyWheelThread = nil end
                WindUI:Notify({ Title = "Auto Buy Wheel", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Equip Best Egg
    local autoEquipBestRunning = false
    local autoEquipBestThread = nil
    local function autoEquipBestLoop()
        while autoEquipBestRunning do
            pcall(function()
                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("PetsService"):WaitForChild("RE"):WaitForChild("EquipBest")
                remote:FireServer()
            end)
            task.wait(1)
        end
    end

    MainTab:Toggle({
        Title = "Auto Equip Best Egg",
        Default = false,
        Callback = function(state)
            autoEquipBestRunning = state
            if state then
                if autoEquipBestThread then task.cancel(autoEquipBestThread) end
                autoEquipBestThread = task.spawn(autoEquipBestLoop)
                WindUI:Notify({ Title = "Auto Equip Best Egg", Content = "Started", Duration = 2, Icon = "zap" })
            else
                if autoEquipBestThread then task.cancel(autoEquipBestThread); autoEquipBestThread = nil end
                WindUI:Notify({ Title = "Auto Equip Best Egg", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    -- Auto Claim Codes
    local claimCodes = {
        "mixpotion",
        "ancient",
        "applepower",
        "coinbanana",
        "charmroot",
        "darkduo",
        "crownpotion",
        "darkegg",
        "eraeggs",
        "grateful"
    }

    local autoClaimRunning = false
    local autoClaimThread = nil
    local autoClaimToggleRef = nil

    local function getClaimRemote()
        local success, remote = pcall(function()
            return ReplicatedStorage
                :WaitForChild("Library")
                :WaitForChild("Knit")
                :WaitForChild("Services")
                :WaitForChild("CodesService")
                :WaitForChild("RF")
                :WaitForChild("Claim")
        end)
        if success then
            return remote
        end
        return nil
    end

    local function claimAllCodes()
        local remote = getClaimRemote()
        if not remote then
            WindUI:Notify({
                Title = "Auto Claim",
                Content = "Remote not found!",
                Duration = 3,
                Icon = "error"
            })
            if autoClaimToggleRef then
                autoClaimToggleRef:SetValue(false)
            end
            return
        end

        local total = #claimCodes
        local claimed = 0
        local failed = 0

        for i, code in ipairs(claimCodes) do
            if not autoClaimRunning then break end

            local args = { code }
            local success, result = pcall(function()
                return remote:InvokeServer(unpack(args))
            end)

            if success then
                claimed = claimed + 1
                WindUI:Notify({
                    Title = "Code Claimed",
                    Content = code,
                    Duration = 1.5,
                    Icon = "check"
                })
            else
                failed = failed + 1
                WindUI:Notify({
                    Title = "Code Failed",
                    Content = code .. " – " .. tostring(result),
                    Duration = 2,
                    Icon = "x"
                })
            end

            task.wait(0.3)
        end

        WindUI:Notify({
            Title = "Auto Claim",
            Content = string.format("Claimed %d / %d codes", claimed, total),
            Duration = 3,
            Icon = claimed == total and "check" or "warning"
        })

        if autoClaimToggleRef then
            autoClaimToggleRef:SetValue(false)
        end
        autoClaimRunning = false
    end

    autoClaimToggleRef = MainTab:Toggle({
        Title = "Auto Claim Codes",
        Default = false,
        Callback = function(state)
            autoClaimRunning = state
            if state then
                if autoClaimThread then
                    task.cancel(autoClaimThread)
                    autoClaimThread = nil
                end
                autoClaimThread = task.spawn(claimAllCodes)
            else
                if autoClaimThread then
                    task.cancel(autoClaimThread)
                    autoClaimThread = nil
                end
                WindUI:Notify({
                    Title = "Auto Claim",
                    Content = "Stopped",
                    Duration = 2,
                    Icon = "stop"
                })
            end
        end
    })

    -- ===== ITEMS TAB (Fruits & Potions) =====
    local ItemsTab = Window:Tab({ Title = "Items", Icon = "box" })

    -- ---------- Fruits ----------
    local fruitsList = {
        "Apple Fruit", "Orange Fruit", "Banana Fruit", "Carrot Fruit", "Blueberry Fruit",
        "Golden Apple Fruit", "Golden Banana Fruit", "Golden Blueberry Fruit", "Golden Carrot Fruit", "Golden Orange Fruit"
    }
    local selectedFruits = {"Apple Fruit"}
    local autoUseFruitRunning = false
    local autoUseFruitThread = nil

    ItemsTab:Dropdown({
        Title = "Select Fruit(s)",
        Desc = "Choose which fruits to auto‑use (multi‑select allowed)",
        Values = fruitsList,
        Value = { "Apple Fruit" },
        Multi = true,
        AllowNone = true,
        Callback = function(option)
            selectedFruits = option
        end
    })

    ItemsTab:Toggle({
        Title = "Auto Use Fruit",
        Default = false,
        Callback = function(state)
            autoUseFruitRunning = state
            if state then
                if #selectedFruits == 0 then
                    WindUI:Notify({ Title = "No fruit selected", Duration = 2, Icon = "alert-circle" })
                    autoUseFruitRunning = false
                    return
                end
                if autoUseFruitThread then task.cancel(autoUseFruitThread) end
                autoUseFruitThread = task.spawn(function()
                    while autoUseFruitRunning do
                        for _, fruit in ipairs(selectedFruits) do
                            if not autoUseFruitRunning then break end
                            local args = { fruit, "Max" }
                            pcall(function()
                                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("InventoryService"):WaitForChild("RE"):WaitForChild("Use")
                                remote:FireServer(unpack(args))
                            end)
                            task.wait(0.2)
                        end
                    end
                end)
                WindUI:Notify({ Title = "Auto Use Fruit", Content = "Started", Duration = 2, Icon = "apple" })
            else
                if autoUseFruitThread then task.cancel(autoUseFruitThread); autoUseFruitThread = nil end
                WindUI:Notify({ Title = "Auto Use Fruit", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })


    -- ---------- Potions ----------
    local potionsList = {
        "Cash Potion", "Champion Potion", "Cocktail Potion", "Power Potion", "Royal Potion"
    }
    local selectedPotions = {"Cash Potion"}
    local autoUsePotionRunning = false
    local autoUsePotionThread = nil

    ItemsTab:Dropdown({
        Title = "Select Potion(s)",
        Desc = "Choose which potions to auto‑use (multi‑select allowed)",
        Values = potionsList,
        Value = { "Cash Potion" },
        Multi = true,
        AllowNone = true,
        Callback = function(option)
            selectedPotions = option
        end
    })

    ItemsTab:Toggle({
        Title = "Auto Use Potion",
        Default = false,
        Callback = function(state)
            autoUsePotionRunning = state
            if state then
                if #selectedPotions == 0 then
                    WindUI:Notify({ Title = "No potion selected", Duration = 2, Icon = "alert-circle" })
                    autoUsePotionRunning = false
                    return
                end
                if autoUsePotionThread then task.cancel(autoUsePotionThread) end
                autoUsePotionThread = task.spawn(function()
                    while autoUsePotionRunning do
                        for _, potion in ipairs(selectedPotions) do
                            if not autoUsePotionRunning then break end
                            local args = { potion, "Max" }
                            pcall(function()
                                local remote = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("InventoryService"):WaitForChild("RE"):WaitForChild("Use")
                                remote:FireServer(unpack(args))
                            end)
                            task.wait(0.2)
                        end
                    end
                end)
                WindUI:Notify({ Title = "Auto Use Potion", Content = "Started", Duration = 2, Icon = "flask" })
            else
                if autoUsePotionThread then task.cancel(autoUsePotionThread); autoUsePotionThread = nil end
                WindUI:Notify({ Title = "Auto Use Potion", Content = "Stopped", Duration = 2, Icon = "stop" })
            end
        end
    })

    Window:Divider()

    -- ===== MISC TAB =====
    local MiscTab = Window:Tab({ Title = "Misc", Icon = "wrench" })

    -- Fly
    MiscTab:Section({ Title = "Fly" })

    local flyEnabled = false
    local flyBodyVelocity, flyBodyGyro, flyLoopThread = nil, nil, nil
    local flySpeed = 50

    local function startFly()
        local char = player.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChild("Humanoid")
        if not root or not hum then return end
        hum.PlatformStand = true
        flyBodyVelocity = Instance.new("BodyVelocity")
        flyBodyVelocity.Velocity = Vector3.new(0,0,0)
        flyBodyVelocity.MaxForce = Vector3.new(1e9,1e9,1e9)
        flyBodyVelocity.Parent = root
        flyBodyGyro = Instance.new("BodyGyro")
        flyBodyGyro.MaxTorque = Vector3.new(1e9,1e9,1e9)
        flyBodyGyro.D = 100
        flyBodyGyro.CFrame = root.CFrame
        flyBodyGyro.Parent = root
        flyLoopThread = task.spawn(function()
            while flyEnabled do
                local c = player.Character
                if not c then break end
                local r = c:FindFirstChild("HumanoidRootPart")
                if not r then break end
                local cam = workspace.CurrentCamera
                local dir = Vector3.new()
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0,1,0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) or UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then dir = dir - Vector3.new(0,1,0) end
                if dir.Magnitude > 0 then flyBodyVelocity.Velocity = dir.Unit * flySpeed else flyBodyVelocity.Velocity = Vector3.new(0,0,0) end
                flyBodyGyro.CFrame = cam.CFrame
                task.wait()
            end
        end)
    end

    local function stopFly()
        if flyBodyVelocity then flyBodyVelocity:Destroy() end
        if flyBodyGyro then flyBodyGyro:Destroy() end
        if flyLoopThread then task.cancel(flyLoopThread) end
        local char = player.Character
        if char then
            local hum = char:FindFirstChild("Humanoid")
            if hum then hum.PlatformStand = false end
        end
    end

    MiscTab:Toggle({
        Title = "Enable Fly",
        Default = false,
        Callback = function(state)
            flyEnabled = state
            if state then startFly() else stopFly() end
        end
    })

    MiscTab:Slider({
        Title = "Fly Speed",
        Desc = "Adjust flight speed (10-200)",
        Step = 1,
        Value = {
            Min = 10,
            Max = 200,
            Default = 50,
        },
        Callback = function(value)
            flySpeed = value
        end
    })

    player.CharacterAdded:Connect(function()
        if flyEnabled then task.wait(1); startFly() end
    end)

    -- Walk Speed
    MiscTab:Section({ Title = "Walk Speed" })

    local walkSpeedValue = 16
    local moveSpeedEnabled = false
    local moveSpeedConn = nil

    MiscTab:Slider({
        Title = "Walk Speed",
        Desc = "Set your movement speed (16-120)",
        Step = 1,
        Value = {
            Min = 16,
            Max = 120,
            Default = 16,
        },
        Callback = function(value)
            walkSpeedValue = value
            if moveSpeedEnabled then
                local char = player.Character
                if char and char:FindFirstChild("Humanoid") then
                    char.Humanoid.WalkSpeed = walkSpeedValue
                end
            end
        end
    })

    MiscTab:Toggle({
        Title = "Enable Speed Boost",
        Default = false,
        Callback = function(state)
            moveSpeedEnabled = state
            local function applySpeed()
                local char = player.Character
                if char and char:FindFirstChild("Humanoid") then
                    char.Humanoid.WalkSpeed = state and walkSpeedValue or 16
                end
            end
            applySpeed()
            if state then
                if moveSpeedConn then moveSpeedConn:Disconnect() end
                moveSpeedConn = player.CharacterAdded:Connect(function(char)
                    task.wait(1)
                    if moveSpeedEnabled and char:FindFirstChild("Humanoid") then
                        char.Humanoid.WalkSpeed = walkSpeedValue
                    end
                end)
            else
                if moveSpeedConn then moveSpeedConn:Disconnect(); moveSpeedConn = nil end
            end
        end
    })

    -- Anti AFK
    MiscTab:Section({ Title = "Anti AFK" })
    MiscTab:Toggle({
        Title = "Prevent AFK Kick",
        Default = false,
        Callback = function(state)
            antiAfkRunning = state
            if state then
                if antiAfkThread then task.cancel(antiAfkThread) end
                antiAfkThread = task.spawn(antiAfkLoop)
            else
                if antiAfkThread then task.cancel(antiAfkThread); antiAfkThread = nil end
            end
        end
    })

    -- ===== SERVER TAB =====
    local ServerTab = Window:Tab({ Title = "Server", Icon = "server" })
    ServerTab:Section({ Title = "Player Info" })
    local avatarUrlServer = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. player.UserId .. "&width=100&height=100&format=png"
    ServerTab:Paragraph({
        Title = "Your Details",
        Desc = string.format("Name: %s\nAge: %d days\nHWID: %s", player.Name, player.AccountAge, hwid),
        Color = Color3.fromHex("#222222"),
        Image = avatarUrlServer,
        ImageSize = 64,
        ThumbnailSize = 80,
    })
    ServerTab:Section({ Title = "Server Actions" })
    ServerTab:Button({ Title = "Hop Server", Icon = "repeat", Callback = function() WindUI:Notify({ Title = "Hopping...", Duration = 2 }); TeleportService:Teleport(game.PlaceId) end })
    ServerTab:Button({ Title = "Rejoin", Icon = "log-in", Callback = function() WindUI:Notify({ Title = "Rejoining...", Duration = 2 }); TeleportService:Teleport(game.PlaceId, player) end })
    ServerTab:Button({ Title = "Reset Character", Icon = "refresh-cw", Callback = function()
        local char = player.Character
        if char then
            char:BreakJoints()
            WindUI:Notify({ Title = "Reset", Content = "Respawning...", Duration = 2, Icon = "refresh-cw" })
        else
            WindUI:Notify({ Title = "Reset", Content = "No character.", Duration = 2, Icon = "info" })
        end
    end})

    -- ===== SUPPORT TAB =====
    local SupportTab = Window:Tab({ Title = "Support", Icon = "heart" })
    local InviteCode = "D5NKtBJEzz"
    local DiscordAPI = "https://discord.com/api/v10/invites/" .. InviteCode .. "?with_counts=true&with_expiration=true"

    local raw = httpGet(DiscordAPI)
    local Response = {}
    if raw then pcall(function() Response = HttpService:JSONDecode(raw) end) end
    local onlineCount = Response and Response.approximate_presence_count or "?"
    local totalCount = Response and Response.approximate_member_count or "?"
    local guildName = (Response and Response.guild and Response.guild.name) or "ZyroYonk Hub"
    local iconUrl = nil
    if Response and Response.guild and Response.guild.id and Response.guild.icon then
        iconUrl = "https://cdn.discordapp.com/icons/" .. tostring(Response.guild.id) .. "/" .. tostring(Response.guild.icon) .. ".png?size=256"
    end
    SupportTab:Section({ Title = "Discord Server" })
    local cardProps = {
        Title = guildName,
        Desc = "Online: " .. tostring(onlineCount) .. "   Members: " .. tostring(totalCount) .. "\nJoin for updates & support.",
        Color = Color3.fromHex("#222222"),
        ImageSize = 64,
        ThumbnailSize = 100,
        Buttons = {
            { Title = "Copy Invite", Callback = function() copyToClipboard("https://discord.gg/" .. InviteCode); WindUI:Notify({ Title = "Copied!", Duration = 2, Icon = "clipboard-check" }) end },
            { Title = "Join Discord", Callback = function() if openUrl then openUrl("https://discord.gg/" .. InviteCode) else copyToClipboard("https://discord.gg/" .. InviteCode); WindUI:Notify({ Title = "Link Copied", Duration = 2, Icon = "globe" }) end end }
        }
    }
    if iconUrl then cardProps.Image = iconUrl end
    SupportTab:Paragraph(cardProps)
    SupportTab:Section({ Title = "Help" })
    SupportTab:Paragraph({ Title = "Executor", Desc = "Using: " .. executorName, Color = Color3.fromHex("#222222") })
    SupportTab:Button({ Title = "Report Bug", Icon = "bug", Callback = function()
        copyToClipboard("Bug Report\nExecutor: " .. executorName .. "\nScript: StarForge Hub v1.0.0\n\nDescribe:")
        WindUI:Notify({ Title = "Template Copied", Content = "Paste in Discord.", Duration = 4, Icon = "message-square" })
    end})
end

showLoadingScreen()
task.wait(2.5)
showGUI()
