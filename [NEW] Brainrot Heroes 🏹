local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer

-- Detect executor
local executor = "Unknown Executor"
if identifyexecutor then
    executor = identifyexecutor()
end

-- ==============================
-- Window
-- ==============================
local Window = WindUI:CreateWindow({
    Title = "[NEW] Brainrot Heroes 🏹",
    Icon = "rocket",
    Author = "ZyroYonk v0.2 ",
    Folder = "ZyroYonkHub",
    Size = UDim2.fromOffset(250,250),
    Theme = "Midnight"
})

WindUI:Notify({
    Title = "⚡ Executor Detected",
    Content = "You are using: "..executor,
    Duration = 5
})

-- ==============================
-- TAB
-- ==============================
local SpawnTab = Window:Tab({
    Title = "Main",
    Icon = "list"
})

SpawnTab:Section({
    Title = "Main Features"
})

--------------------------------------------------
-- SERVICES
--------------------------------------------------

local heroCenter = workspace:WaitForChild("HeroCenter")

local character = player.Character or player.CharacterAdded:Wait()
local hrp = character:WaitForChild("HumanoidRootPart")

player.CharacterAdded:Connect(function(char)
    character = char
    hrp = char:WaitForChild("HumanoidRootPart")
end)

--------------------------------------------------
-- HERO GROUPS
--------------------------------------------------

local HeroGroups = {
    Rare = {"Archer"},
    Epic = {"Ninja","Mage","Viking"},
    Legendary = {"Gunslinger","Assassin","Alchemist"},
    Mythic = {"Necromancer","Samurai","Paladin"},
    Godly = {"Caveman","Bomber"},
    Secret = {"Chef","Cyborg","Druid","Mummy"},
    Celestial = {"Wraith","Cthulhu"}
}

--------------------------------------------------
-- STATS
--------------------------------------------------

local Stats = {
    Total = 0,
    Rare = 0,
    Epic = 0,
    Legendary = 0,
    Mythic = 0,
    Godly = 0,
    Secret = 0,
    Celestial = 0
}

local StartTime = os.time()

local StatsBox = SpawnTab:Paragraph({
    Title = "Session Stats",
    Desc = "Loading..."
})

local function formatTime(seconds)
    local h = math.floor(seconds/3600)
    local m = math.floor((seconds%3600)/60)
    local s = seconds%60
    return string.format("%02d:%02d:%02d",h,m,s)
end

task.spawn(function()
    while true do

        local playtime = os.time()-StartTime

        local text =
        "⏱ Time Played: "..formatTime(playtime).."\n\n"..
        "🏹 Total Heroes: "..Stats.Total.."\n\n"..
        "Rare: "..Stats.Rare.."\n"..
        "Epic: "..Stats.Epic.."\n"..
        "Legendary: "..Stats.Legendary.."\n"..
        "Mythic: "..Stats.Mythic.."\n"..
        "Godly: "..Stats.Godly.."\n"..
        "Secret: "..Stats.Secret.."\n"..
        "Celestial: "..Stats.Celestial

        pcall(function()
            StatsBox:SetDesc(text)
        end)

        task.wait(1)
    end
end)

--------------------------------------------------
-- VARIABLES
--------------------------------------------------

local SelectedRarities = {}
local AutoBuyEnabled = false

--------------------------------------------------
-- BUY HERO FUNCTION
--------------------------------------------------

local function buyHero(heroName)

    local hero = heroCenter:FindFirstChild(heroName)
    if not hero then return end

    hrp.CFrame = hero:GetPivot() * CFrame.new(0,3,0)
    task.wait(0.5)

    local prompt = hero:FindFirstChildWhichIsA("ProximityPrompt", true)

    if prompt and prompt.Enabled then
        fireproximityprompt(prompt,0.5)

        for rarity,group in pairs(HeroGroups) do
            for _,name in ipairs(group) do
                if name == heroName then
                    Stats[rarity] += 1
                    Stats.Total += 1
                end
            end
        end

        task.wait(0.6)
    end

    local weapon = hero:FindFirstChild("Weapon")

    if weapon then
        hrp.CFrame = weapon:GetPivot() * CFrame.new(0,3,0)
        task.wait(0.5)

        local weaponPrompt = weapon:FindFirstChildWhichIsA("ProximityPrompt", true)

        if weaponPrompt and weaponPrompt.Enabled then
            fireproximityprompt(weaponPrompt,0.5)
        end
    end

end

--------------------------------------------------
-- DROPDOWN
--------------------------------------------------

SpawnTab:Dropdown({
    Title = "Select Hero Rarities",
    Values = {"Rare","Epic","Legendary","Mythic","Godly","Secret","Celestial"},
    Multi = true,
    Default = {},
    Callback = function(values)

        SelectedRarities = {}

        for _,v in ipairs(values) do
            SelectedRarities[v] = true
        end

    end
})

--------------------------------------------------
-- AUTO BUY
--------------------------------------------------

SpawnTab:Toggle({
    Title = "Auto Buy Selected Heroes",
    Default = false,
    Callback = function(state)

        AutoBuyEnabled = state

        if state then
            task.spawn(function()

                while AutoBuyEnabled do

                    for rarity,_ in pairs(SelectedRarities) do
                        local group = HeroGroups[rarity]

                        if group then
                            for _,heroName in ipairs(group) do
                                buyHero(heroName)
                                task.wait(0.2)
                            end
                        end
                    end

                    task.wait(0.5)

                end

            end)
        end
    end
})


-- ==============================
-- AUTO EQUIP BEST BRAINROTS TOGGLE
-- ==============================
local AutoEquipEnabled = false

SpawnTab:Toggle({
    Title = "Auto Equip Best Brainrots",
    Default = false,
    Callback = function(state)
        AutoEquipEnabled = state

        if state then
            task.spawn(function()
                while AutoEquipEnabled do
                    -- Call the server to equip the best brainrots
                    local success, err = pcall(function()
                        game:GetService("ReplicatedStorage")
                            :WaitForChild("Remotes")
                            :WaitForChild("EquipBestBrainrots")
                            :InvokeServer()
                    end)
                    if not success then
                        warn("Failed to equip brainrots: "..tostring(err))
                    end

                    task.wait(2)
                end
            end)
        end
    end
})

--------------------------------------------------
-- AUTO COLLECT CASH
--------------------------------------------------

local EnabledIncome = false

SpawnTab:Toggle({
    Title = "Auto Collect Cash",
    Default = false,
    Callback = function(state)

        EnabledIncome = state

        if state then
            task.spawn(function()
                while EnabledIncome do

                    for i = 1,30 do
                        local args = {tostring(i)}

                        game:GetService("ReplicatedStorage")
                        :WaitForChild("Remotes")
                        :WaitForChild("CollectBrainrotIncome")
                        :InvokeServer(unpack(args))

                        task.wait(0.2)
                    end

                    task.wait(1)

                end
            end)
        end

    end
})

--------------------------------------------------
-- AUTO REBIRTH
--------------------------------------------------

local EnabledRebirth = false

SpawnTab:Toggle({
    Title = "Auto Rebirth",
    Default = false,
    Callback = function(state)

        EnabledRebirth = state

        if state then
            task.spawn(function()

                while EnabledRebirth do

                    game:GetService("ReplicatedStorage")
                    :WaitForChild("Remotes")
                    :WaitForChild("AttemptRebirth")
                    :InvokeServer()

                    task.wait(3)

                end

            end)
        end

    end
})

--------------------------------------------------
-- EXTRA FEATURES SECTION
--------------------------------------------------

SpawnTab:Section({
    Title = "Extra Features"
})

--------------------------------------------------
-- FLY SYSTEM
--------------------------------------------------

local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local camera = workspace.CurrentCamera

local character = player.Character or player.CharacterAdded:Wait()
local hrp = character:WaitForChild("HumanoidRootPart")

local fly = false
local pos = hrp.CFrame
local speed = 30
local keysDown = {}

player.CharacterAdded:Connect(function(char)
    character = char
    hrp = char:WaitForChild("HumanoidRootPart")
    pos = hrp.CFrame
end)

--------------------------------------------------
-- KEY DETECTION
--------------------------------------------------

UserInputService.InputBegan:Connect(function(input,gp)
    if gp then return end
    if input.UserInputType == Enum.UserInputType.Keyboard then
        keysDown[input.KeyCode] = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Keyboard then
        keysDown[input.KeyCode] = nil
    end
end)

--------------------------------------------------
-- FLY MOVEMENT
--------------------------------------------------

RunService.RenderStepped:Connect(function(dt)

    if not fly or not hrp then return end

    local moveVector = Vector3.zero

    if keysDown[Enum.KeyCode.W] then moveVector += Vector3.new(0,0,-1) end
    if keysDown[Enum.KeyCode.S] then moveVector += Vector3.new(0,0,1) end
    if keysDown[Enum.KeyCode.A] then moveVector += Vector3.new(-1,0,0) end
    if keysDown[Enum.KeyCode.D] then moveVector += Vector3.new(1,0,0) end
    if keysDown[Enum.KeyCode.Space] then moveVector += Vector3.new(0,1,0) end
    if keysDown[Enum.KeyCode.LeftShift] then moveVector += Vector3.new(0,-1,0) end

    if moveVector.Magnitude > 0 then

        local camCF = CFrame.new(Vector3.zero,camera.CFrame.LookVector)
        local direction = camCF:VectorToWorldSpace(moveVector).Unit

        pos += direction * speed * dt

    end

end)

RunService.Heartbeat:Connect(function()

    if fly and hrp then
        hrp.CFrame = CFrame.new(pos.Position)
    end

end)

--------------------------------------------------
-- FLY TOGGLE
--------------------------------------------------

SpawnTab:Toggle({
    Title = "Fly",
    Default = false,
    Callback = function(state)

        fly = state

        if hrp then
            hrp.Anchored = state

            if state then
                pos = hrp.CFrame
            end
        end

    end
})

--------------------------------------------------
-- FLY SPEED INPUT (REPLACED SLIDER)
--------------------------------------------------

SpawnTab:Input({
    Title = "Fly Speed",
    Placeholder = "Enter Speed (Default 30)",
    Callback = function(text)

        local num = tonumber(text)

        if num then
            speed = num
        end

    end
})

--------------------------------------------------
-- WALK SPEED INPUT (REPLACED SLIDER)
--------------------------------------------------

SpawnTab:Input({
    Title = "WalkSpeed",
    Placeholder = "Enter WalkSpeed (16 - 500)",
    Callback = function(text)

        local value = tonumber(text)

        if value then

            local char = player.Character or player.CharacterAdded:Wait()
            local humanoid = char:FindFirstChildOfClass("Humanoid")

            if humanoid then
                humanoid.WalkSpeed = value
            end

        end

    end
})


-- Create Tab
local MainTab = Window:Tab({
    Title = "Support",
    Icon = "heart"
})

-- Paragraph (THIS WILL SHOW)
MainTab:Paragraph({
    Title = "Support Our Project ❤️",
    Desc = "If you enjoy using this Script, please join our Discord server. Your support helps us improve and create better scripts for everyone! And dont forget to follow my Rscript"
})

-- Copy Button
MainTab:Button({
    Title = "Copy Discord Link",
    Callback = function()

        local link = "https://discord.gg/R7KF6k4pn8"

        if setclipboard then
            setclipboard(link)
        end

        WindUI:Notify({
            Title = "Link Copied ✅",
            Content = "Thanks for supporting us! The link is now in your clipboard.",
            Duration = 4
        })

    end
})

-- Copy Button
MainTab:Button({
    Title = "Copy Link To Follow My Rscript",
    Callback = function()

        local link = "https://rscripts.net/@ZyroYonk"

        if setclipboard then
            setclipboard(link)
        end

        WindUI:Notify({
            Title = "Link Copied ✅",
            Content = "Thanks for supporting us! The link is now in your clipboard.",
            Duration = 4
        })

    end
})
