local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local player = Players.LocalPlayer

-- ================== EXECUTOR DETECTION ==================
local function getExecutorName()
    if identifyexecutor then
        local success, name = pcall(identifyexecutor)
        if success and name and name ~= "" then return name end
    end
    if getexecutorname then
        local success, name = pcall(getexecutorname)
        if success and name and name ~= "" then return name end
    end
    if syn and syn.crypt and syn.crypt.custom then return "Synapse" end
    if krnl then return "Krnl" end
    if fluxus then return "Fluxus" end
    if electron then return "Electron" end
    if is_sirhurt_closure then return "SirHurt" end
    if getgenv and getgenv().IS_SCRIPT_WARE then return "ScriptWare" end
    if getgenv and getgenv().IS_VEGA_X then return "Vega X" end
    if getgenv and getgenv().IS_DELTA then return "Delta" end
    if getgenv and getgenv().IS_CODE_X then return "Code X" end
    return "Unknown Executor"
end

local executorName = getExecutorName()

-- ================== ROBUST LIVE PING (MULTIPLE METHODS) ==================
local function getPing()
    local success, stat = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then
        return math.floor(stat)
    end
    success, stat = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Ping"]:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then
        return math.floor(stat)
    end
    success, stat = pcall(function()
        return game:GetService("Stats").Network.Ping:GetValue()
    end)
    if success and type(stat) == "number" and stat > 0 then
        return math.floor(stat)
    end
    local sent = tick()
    local dummy = Instance.new("Part")
    dummy.Parent = workspace
    local received = tick()
    dummy:Destroy()
    local estimate = math.floor((received - sent) * 500)
    if estimate > 0 then return estimate end
    return 0
end

task.wait(1)
WindUI:Notify({
    Title = "Executor Detected",
    Content = "You are using: " .. executorName,
    Duration = 5,
    Icon = "terminal"
})

-- ================== COPY TO CLIPBOARD ==================
local function copyToClipboard(text)
    if setclipboard then
        setclipboard(text)
    elseif syn and syn.write_clipboard then
        syn.write_clipboard(text)
    elseif toclipboard then
        toclipboard(text)
    else
        pcall(function()
            local input = Instance.new("TextBox")
            input.Text = text
            input.Parent = game:GetService("CoreGui")
            input:CaptureFocus()
            input:SelectAll()
            task.wait(0.1)
            input.Parent = nil
        end)
    end
end

-- ================== THEME ==================
WindUI:AddTheme({
    Name = "ZyroTheme",
    Font = Font.fromName("GothamSSm", Enum.FontWeight.Medium),
    Background = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#1a1a1a") },
        ["100"] = { Color = Color3.fromHex("#1a1a1a") }
    }, { Rotation = 180 }),
    Accent = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#2563eb") },
        ["100"] = { Color = Color3.fromHex("#1d4ed8") }
    }, { Rotation = 90 }),
    SideBar = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#141414") },
        ["100"] = { Color = Color3.fromHex("#141414") }
    }, { Rotation = 180 }),
    Tab = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#222222") },
        ["100"] = { Color = Color3.fromHex("#222222") }
    }, { Rotation = 90 }),
    Toggle = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#16a34a") },
        ["100"] = { Color = Color3.fromHex("#15803d") }
    }, { Rotation = 90 }),
    Button = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#2563eb") },
        ["100"] = { Color = Color3.fromHex("#1e40af") }
    }, { Rotation = 135 }),
    Text = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#ffffff") },
        ["100"] = { Color = Color3.fromHex("#d1d5db") }
    }, { Rotation = 90 }),
    Icon = WindUI:Gradient({
        ["0"]   = { Color = Color3.fromHex("#ffffff") },
        ["100"] = { Color = Color3.fromHex("#9ca3af") }
    }, { Rotation = 90 }),
})

-- ================== LOADING SCREEN ==================
local function showLoadingScreen()
    local blur = Instance.new("BlurEffect", game:GetService("Lighting"))
    blur.Size = 0
    TweenService:Create(blur, TweenInfo.new(0.6), { Size = 18 }):Play()

    local screenGui = Instance.new("ScreenGui")
    screenGui.Parent = player:WaitForChild("PlayerGui")
    screenGui.Name = "ZyroYonkIntro"
    screenGui.ResetOnSpawn = false
    screenGui.IgnoreGuiInset = true

    local frame = Instance.new("Frame", screenGui)
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1

    local bg = Instance.new("Frame", frame)
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(25, 0, 40)
    bg.BackgroundTransparency = 1
    TweenService:Create(bg, TweenInfo.new(0.8), { BackgroundTransparency = 0.2 }):Play()

    local word = "StarForge Hub"
    local letters = {}

    for i = 1, #word do
        local char = word:sub(i, i)
        local label = Instance.new("TextLabel")
        label.Text = char
        label.Font = Enum.Font.GothamBlack
        label.TextColor3 = Color3.new(1, 1, 1)
        label.TextTransparency = 1
        label.TextSize = 20
        label.Size = UDim2.new(0, 60, 0, 60)
        label.AnchorPoint = Vector2.new(0.5, 0.5)
        label.Position = UDim2.new(0.5, (i - (#word / 2 + 0.5)) * 55, 0.5, 20)
        label.BackgroundTransparency = 1
        label.Parent = frame

        local gradient = Instance.new("UIGradient")
        gradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(200, 120, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 0, 200))
        })
        gradient.Rotation = 90
        gradient.Parent = label

        TweenService:Create(label, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            TextTransparency = 0,
            TextSize = 55,
            Position = UDim2.new(0.5, (i - (#word / 2 + 0.5)) * 55, 0.5, 0)
        }):Play()

        table.insert(letters, label)
        task.wait(0.12)
    end

    task.wait(2)

    for _, label in pairs(letters) do
        TweenService:Create(label, TweenInfo.new(0.4), { TextTransparency = 1, TextSize = 20 }):Play()
    end

    TweenService:Create(bg, TweenInfo.new(0.6), { BackgroundTransparency = 1 }):Play()
    TweenService:Create(blur, TweenInfo.new(0.6), { Size = 0 }):Play()
    task.wait(0.7)
    screenGui:Destroy()
    blur:Destroy()
end

-- ================== MAIN GUI ==================
local function showGUI()
    local coolTitle = "Dumpling Squishy Race🥟"
    local profileIconUrl = "https://i.pinimg.com/736x/37/3a/b3/373ab3e072178f47dcb7fdafaddb6854.jpg"

    local Window = WindUI:CreateWindow({
        Title = coolTitle,
        Icon = "swords",
        Author = "By Vloods" .. utf8.char(0xE000),
        Folder = "ZyroYonk Hub",
        Size = UDim2.fromOffset(560, 380),
        MinSize = Vector2.new(560, 350),
        MaxSize = Vector2.new(850, 560),
        Transparent = false,
        Theme = "ZyroTheme",
        Resizable = true,
        SideBarWidth = 200,
        HideSearchBar = false,
        ScrollBarEnabled = false,
        User = { Enabled = true, Anonymous = false, Callback = function() end },
                KeySystem = {                                                   
            Note = "Enter your PlatoBoost key to access StarForge Hub.",              
            API = {                                                     
                { -- PlatoBoost
                    Type = "platoboost",                                
                    ServiceId = 22001, -- Your PlatoBoost service ID
                    Secret = "945af45d-5779-4a05-a31c-432930563763", 
                },                                                      
            },                                                          
        }
    })

    -- Tags: version + live ping
    local versionTag = Window:Tag({
        Title = "v1.0.6",
        Icon = "tag",
        Color = Color3.fromHex("#2563eb"),
        Radius = 100
    })

    task.spawn(function()
        while true do
            local ping = getPing()
            if versionTag then
                versionTag:SetTitle("v1.0.6 | " .. ping .. "ms")
            end
            task.wait(0.1)
        end
    end)

    Window:EditOpenButton({
        Title = "Open ZyroYonk Hub",
        Icon = "swords",
        CornerRadius = UDim.new(0, 8),
        StrokeThickness = 1,
        Color = ColorSequence.new(Color3.fromHex("#2563eb"), Color3.fromHex("#1d4ed8")),
        OnlyMobile = false,
        Enabled = true,
        Draggable = true,
    })

    -- ========== CHANGELOG TAB ==========
    local ChangelogTab = Window:Tab({ Title = "Changelog", Icon = "scroll" })
    ChangelogTab:Paragraph({
        Title = "Update v1.0.1",
        Desc = "- Initial release",
        Color = Color3.fromHex("#222222"),
        Thumbnail = profileIconUrl,
        ThumbnailSize = 100,
    })

    -- ========== MAIN FEATURES TAB ==========
    local MainTab = Window:Tab({ Title = "Main Features", Icon = "house" })

    -- ========== RACE SECTION ==========
    MainTab:Section({ Title = "Race" })

    local autoWinsEnabled = false
    local autoWinsThread = nil

    local function startRace()
        pcall(function()
            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("RaceService"):WaitForChild("RF"):WaitForChild("RequestStartRace"):InvokeServer()
        end)
    end

    local function runPath()
        pcall(function()
            local character = player.Character or player.CharacterAdded:Wait()
            local hrp = character:WaitForChild("HumanoidRootPart")
            
            local folder = workspace.Func["奖励区域"]
            
            local function getParts()
                local parts = {}
                for _, obj in ipairs(folder:GetDescendants()) do
                    if obj:IsA("BasePart") then
                        table.insert(parts, obj)
                    end
                end
                return parts
            end
            
            local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Linear)
            local parts = getParts()
            
            for _, part in ipairs(parts) do
                if part and part.Parent then
                    local tween = TweenService:Create(hrp, tweenInfo, {
                        CFrame = part.CFrame + Vector3.new(0, 3, 0)
                    })
                    tween:Play()
                    tween.Completed:Wait()
                end
            end
        end)
    end

    MainTab:Toggle({
        Title = "Auto Wins",
        Desc = "Automatically starts race and runs the reward path",
        Default = false,
        Callback = function(state)
            autoWinsEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Wins ON", Content = "Starting race and running path...", Duration = 3, Icon = "trophy" })
                autoWinsThread = task.spawn(function()
                    while autoWinsEnabled do
                        pcall(function()
                            startRace()
                            task.wait(1)
                            runPath()
                        end)
                        task.wait(0.1)
                    end
                end)
            else
                if autoWinsThread then task.cancel(autoWinsThread) autoWinsThread = nil end
                WindUI:Notify({ Title = "Auto Wins OFF", Content = "Stopped auto wins", Duration = 2, Icon = "trophy-off" })
            end
        end
    })

    -- ========== REBIRTH SECTION ==========
    MainTab:Section({ Title = "Rebirth" })

    local autoRebirthEnabled = false
    local autoRebirthThread = nil

    MainTab:Toggle({
        Title = "Auto Rebirth",
        Desc = "Automatically rebirths when available",
        Default = false,
        Callback = function(state)
            autoRebirthEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Rebirth ON", Content = "Rebirthing automatically", Duration = 3, Icon = "refresh-cw" })
                autoRebirthThread = task.spawn(function()
                    while autoRebirthEnabled do
                        pcall(function()
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("RebirthService"):WaitForChild("RE"):WaitForChild("RequestRebirth"):FireServer()
                        end)
                        task.wait(1)
                    end
                end)
            else
                if autoRebirthThread then task.cancel(autoRebirthThread) autoRebirthThread = nil end
                WindUI:Notify({ Title = "Auto Rebirth OFF", Content = "Stopped rebirthing", Duration = 2, Icon = "refresh-cw-off" })
            end
        end
    })

    -- ========== AUTO BUY SQUISHY SECTION ==========
    MainTab:Section({ Title = "Auto Buy Squishy" })

    local autoBuySquishyEnabled = false
    local autoBuySquishyThread = nil

    local function buyAllSquishy()
        for i = 2, 19 do
            pcall(function()
                local args = { "Mount_" .. i }
                game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("MountService"):WaitForChild("RF"):WaitForChild("Cost"):InvokeServer(unpack(args))
            end)
            task.wait(0.1)
        end
    end

    MainTab:Toggle({
        Title = "Auto Buy Squishy",
        Desc = "Automatically buys all Squishy mounts from 2-19",
        Default = false,
        Callback = function(state)
            autoBuySquishyEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Buy Squishy ON", Content = "Buying all Squishy mounts", Duration = 3, Icon = "shopping-cart" })
                autoBuySquishyThread = task.spawn(function()
                    while autoBuySquishyEnabled do
                        buyAllSquishy()
                        task.wait(2)
                    end
                end)
            else
                if autoBuySquishyThread then task.cancel(autoBuySquishyThread) autoBuySquishyThread = nil end
                WindUI:Notify({ Title = "Auto Buy Squishy OFF", Content = "Stopped buying Squishy", Duration = 2, Icon = "shopping-cart-off" })
            end
        end
    })

    -- ========== AUTO EQUIP BEST PET SECTION ==========
    MainTab:Section({ Title = "Auto Equip Best Pet" })

    local autoEquipBestEnabled = false
    local autoEquipBestThread = nil

    MainTab:Toggle({
        Title = "Auto Equip Best Pet",
        Desc = "Automatically equips your best pet",
        Default = false,
        Callback = function(state)
            autoEquipBestEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Equip Best ON", Content = "Equipping best pet", Duration = 3, Icon = "user-plus" })
                autoEquipBestThread = task.spawn(function()
                    while autoEquipBestEnabled do
                        pcall(function()
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("PetService"):WaitForChild("RF"):WaitForChild("EquipBest"):InvokeServer()
                        end)
                        task.wait(2)
                    end
                end)
            else
                if autoEquipBestThread then task.cancel(autoEquipBestThread) autoEquipBestThread = nil end
                WindUI:Notify({ Title = "Auto Equip Best OFF", Content = "Stopped equipping", Duration = 2, Icon = "user-minus" })
            end
        end
    })

    -- ========== AUTO UPGRADE SECTION ==========
    MainTab:Section({ Title = "Auto Upgrade" })

    -- Upgrade: MoreWim
    local autoMoreWimEnabled = false
    local autoMoreWimThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: MoreWim",
        Desc = "Automatically upgrades MoreWim",
        Default = false,
        Callback = function(state)
            autoMoreWimEnabled = state
            if state then
                WindUI:Notify({ Title = "MoreWim Upgrade ON", Content = "Upgrading MoreWim", Duration = 2, Icon = "arrow-up" })
                autoMoreWimThread = task.spawn(function()
                    while autoMoreWimEnabled do
                        pcall(function()
                            local args = { "MoreWim" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoMoreWimThread then task.cancel(autoMoreWimThread) autoMoreWimThread = nil end
                WindUI:Notify({ Title = "MoreWim Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- Upgrade: MaxSpeed
    local autoMaxSpeedEnabled = false
    local autoMaxSpeedThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: MaxSpeed",
        Desc = "Automatically upgrades MaxSpeed",
        Default = false,
        Callback = function(state)
            autoMaxSpeedEnabled = state
            if state then
                WindUI:Notify({ Title = "MaxSpeed Upgrade ON", Content = "Upgrading MaxSpeed", Duration = 2, Icon = "arrow-up" })
                autoMaxSpeedThread = task.spawn(function()
                    while autoMaxSpeedEnabled do
                        pcall(function()
                            local args = { "MaxSpeed" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoMaxSpeedThread then task.cancel(autoMaxSpeedThread) autoMaxSpeedThread = nil end
                WindUI:Notify({ Title = "MaxSpeed Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- Upgrade: MorePower
    local autoMorePowerEnabled = false
    local autoMorePowerThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: MorePower",
        Desc = "Automatically upgrades MorePower",
        Default = false,
        Callback = function(state)
            autoMorePowerEnabled = state
            if state then
                WindUI:Notify({ Title = "MorePower Upgrade ON", Content = "Upgrading MorePower", Duration = 2, Icon = "arrow-up" })
                autoMorePowerThread = task.spawn(function()
                    while autoMorePowerEnabled do
                        pcall(function()
                            local args = { "MorePower" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoMorePowerThread then task.cancel(autoMorePowerThread) autoMorePowerThread = nil end
                WindUI:Notify({ Title = "MorePower Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- Upgrade: Acceleration
    local autoAccelerationEnabled = false
    local autoAccelerationThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: Acceleration",
        Desc = "Automatically upgrades Acceleration",
        Default = false,
        Callback = function(state)
            autoAccelerationEnabled = state
            if state then
                WindUI:Notify({ Title = "Acceleration Upgrade ON", Content = "Upgrading Acceleration", Duration = 2, Icon = "arrow-up" })
                autoAccelerationThread = task.spawn(function()
                    while autoAccelerationEnabled do
                        pcall(function()
                            local args = { "Acceleration" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoAccelerationThread then task.cancel(autoAccelerationThread) autoAccelerationThread = nil end
                WindUI:Notify({ Title = "Acceleration Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- Upgrade: MoreGem
    local autoMoreGemEnabled = false
    local autoMoreGemThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: MoreGem",
        Desc = "Automatically upgrades MoreGem",
        Default = false,
        Callback = function(state)
            autoMoreGemEnabled = state
            if state then
                WindUI:Notify({ Title = "MoreGem Upgrade ON", Content = "Upgrading MoreGem", Duration = 2, Icon = "arrow-up" })
                autoMoreGemThread = task.spawn(function()
                    while autoMoreGemEnabled do
                        pcall(function()
                            local args = { "MoreGem" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoMoreGemThread then task.cancel(autoMoreGemThread) autoMoreGemThread = nil end
                WindUI:Notify({ Title = "MoreGem Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- Upgrade: GemChance
    local autoGemChanceEnabled = false
    local autoGemChanceThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: GemChance",
        Desc = "Automatically upgrades GemChance",
        Default = false,
        Callback = function(state)
            autoGemChanceEnabled = state
            if state then
                WindUI:Notify({ Title = "GemChance Upgrade ON", Content = "Upgrading GemChance", Duration = 2, Icon = "arrow-up" })
                autoGemChanceThread = task.spawn(function()
                    while autoGemChanceEnabled do
                        pcall(function()
                            local args = { "GemChance" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoGemChanceThread then task.cancel(autoGemChanceThread) autoGemChanceThread = nil end
                WindUI:Notify({ Title = "GemChance Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- Upgrade: EggLuck
    local autoEggLuckEnabled = false
    local autoEggLuckThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: EggLuck",
        Desc = "Automatically upgrades EggLuck",
        Default = false,
        Callback = function(state)
            autoEggLuckEnabled = state
            if state then
                WindUI:Notify({ Title = "EggLuck Upgrade ON", Content = "Upgrading EggLuck", Duration = 2, Icon = "arrow-up" })
                autoEggLuckThread = task.spawn(function()
                    while autoEggLuckEnabled do
                        pcall(function()
                            local args = { "EggLuck" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoEggLuckThread then task.cancel(autoEggLuckThread) autoEggLuckThread = nil end
                WindUI:Notify({ Title = "EggLuck Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- Upgrade: StartSpeed
    local autoStartSpeedEnabled = false
    local autoStartSpeedThread = nil

    MainTab:Toggle({
        Title = "Auto Upgrade: StartSpeed",
        Desc = "Automatically upgrades StartSpeed",
        Default = false,
        Callback = function(state)
            autoStartSpeedEnabled = state
            if state then
                WindUI:Notify({ Title = "StartSpeed Upgrade ON", Content = "Upgrading StartSpeed", Duration = 2, Icon = "arrow-up" })
                autoStartSpeedThread = task.spawn(function()
                    while autoStartSpeedEnabled do
                        pcall(function()
                            local args = { "StartSpeed" }
                            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("UpgradeService"):WaitForChild("RF"):WaitForChild("UpgradeWithGem"):InvokeServer(unpack(args))
                        end)
                        task.wait(0.5)
                    end
                end)
            else
                if autoStartSpeedThread then task.cancel(autoStartSpeedThread) autoStartSpeedThread = nil end
                WindUI:Notify({ Title = "StartSpeed Upgrade OFF", Content = "Stopped upgrading", Duration = 2, Icon = "arrow-down" })
            end
        end
    })

    -- ========== AUTO CLAIM REWARDS SECTION ==========
    MainTab:Section({ Title = "Auto Claim Rewards" })

    local autoClaimRewardsEnabled = false
    local autoClaimRewardsThread = nil

    local function claimAllRewards()
        for i = 1, 12 do
            pcall(function()
                local args = { i }
                game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("OnlineRewardService"):WaitForChild("RF"):WaitForChild("Claim"):InvokeServer(unpack(args))
            end)
            task.wait(0.2)
        end
    end

    MainTab:Toggle({
        Title = "Auto Claim Reward 1-12",
        Desc = "Automatically claims all available rewards from 1 to 12",
        Default = false,
        Callback = function(state)
            autoClaimRewardsEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Claim Rewards ON", Content = "Claiming rewards 1-12...", Duration = 3, Icon = "gift" })
                autoClaimRewardsThread = task.spawn(function()
                    while autoClaimRewardsEnabled do
                        claimAllRewards()
                        task.wait(5)
                    end
                end)
            else
                if autoClaimRewardsThread then task.cancel(autoClaimRewardsThread) autoClaimRewardsThread = nil end
                WindUI:Notify({ Title = "Auto Claim Rewards OFF", Content = "Stopped claiming rewards", Duration = 2, Icon = "gift-off" })
            end
        end
    })

    -- ========== AUTO BUY TRAILS SECTION ==========
    MainTab:Section({ Title = "Auto Buy Trails" })

    local autoBuyTrailsEnabled = false
    local autoBuyTrailsThread = nil

    local function buyAllTrails()
        for i = 1, 25 do
            pcall(function()
                local args = { "Trail_" .. i }
                game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("TrailService"):WaitForChild("RF"):WaitForChild("Cost"):InvokeServer(unpack(args))
            end)
            task.wait(0.1)
        end
    end

    MainTab:Toggle({
        Title = "Auto Buy Trails 1-25",
        Desc = "Automatically buys all trails from 1 to 25",
        Default = false,
        Callback = function(state)
            autoBuyTrailsEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Buy Trails ON", Content = "Buying trails 1-25...", Duration = 3, Icon = "sparkles" })
                autoBuyTrailsThread = task.spawn(function()
                    while autoBuyTrailsEnabled do
                        buyAllTrails()
                        task.wait(3)
                    end
                end)
            else
                if autoBuyTrailsThread then task.cancel(autoBuyTrailsThread) autoBuyTrailsThread = nil end
                WindUI:Notify({ Title = "Auto Buy Trails OFF", Content = "Stopped buying trails", Duration = 2, Icon = "sparkles-off" })
            end
        end
    })

    -- ========== AUTO BUY PARTNER SECTION ==========
    MainTab:Section({ Title = "Auto Buy Partner" })

    local autoBuyPartnerEnabled = false
    local autoBuyPartnerThread = nil

    local function buyAllPartners()
        for i = 1, 10 do
            pcall(function()
                local args = { "Partner_" .. i }
                game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("PartnerService"):WaitForChild("RF"):WaitForChild("Cost"):InvokeServer(unpack(args))
            end)
            task.wait(0.1)
        end
    end

    MainTab:Toggle({
        Title = "Auto Buy Partner 1-10",
        Desc = "Automatically buys all partners from 1 to 10",
        Default = false,
        Callback = function(state)
            autoBuyPartnerEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Buy Partner ON", Content = "Buying partners 1-10...", Duration = 3, Icon = "users" })
                autoBuyPartnerThread = task.spawn(function()
                    while autoBuyPartnerEnabled do
                        buyAllPartners()
                        task.wait(3)
                    end
                end)
            else
                if autoBuyPartnerThread then task.cancel(autoBuyPartnerThread) autoBuyPartnerThread = nil end
                WindUI:Notify({ Title = "Auto Buy Partner OFF", Content = "Stopped buying partners", Duration = 2, Icon = "users-off" })
            end
        end
    })

    -- ========== AUTO CLAIM ACHIEVEMENTS SECTION ==========
    MainTab:Section({ Title = "Auto Claim Achievements" })

    local autoClaimAchievementsEnabled = false
    local autoClaimAchievementsThread = nil

    local achievementsList = {
        "TotalWins",
        "HighestPower",
        "EggsHatched",
        "RaceCompleted",
        "PlayTime",
        "Rebirths",
        "UnlockedTrainers",
        "UnlockedMounts"
    }

    local function claimAllAchievements()
        for _, achievement in ipairs(achievementsList) do
            pcall(function()
                local args = { achievement }
                game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("AchievementService"):WaitForChild("RF"):WaitForChild("ClaimStage"):InvokeServer(unpack(args))
            end)
            task.wait(0.2)
        end
    end

    MainTab:Toggle({
        Title = "Auto Claim Achievements",
        Desc = "Automatically claims all achievements (TotalWins, HighestPower, EggsHatched, RaceCompleted, PlayTime, Rebirths, UnlockedTrainers, UnlockedMounts)",
        Default = false,
        Callback = function(state)
            autoClaimAchievementsEnabled = state
            if state then
                WindUI:Notify({ Title = "Auto Claim Achievements ON", Content = "Claiming all achievements...", Duration = 3, Icon = "medal" })
                autoClaimAchievementsThread = task.spawn(function()
                    while autoClaimAchievementsEnabled do
                        claimAllAchievements()
                        task.wait(5)
                    end
                end)
            else
                if autoClaimAchievementsThread then task.cancel(autoClaimAchievementsThread) autoClaimAchievementsThread = nil end
                WindUI:Notify({ Title = "Auto Claim Achievements OFF", Content = "Stopped claiming achievements", Duration = 2, Icon = "medal-off" })
            end
        end
    })

    -- ========== AUTO HATCH EGGS SECTION ==========
    MainTab:Section({ Title = "Auto Hatch Eggs" })

    local autoHatchEggsEnabled = false
    local autoHatchEggsThread = nil
    local selectedEggs = { "Egg1" } -- Default: Basic Egg

    local eggsList = { "Basic Egg", "Rare Egg", "Epic Egg", "Legendary Egg" }
    local eggsValues = {
        ["Basic Egg"] = "Egg1",
        ["Rare Egg"] = "Egg2",
        ["Epic Egg"] = "Egg3",
        ["Legendary Egg"] = "Egg4"
    }

    local function hatchEgg(eggValue)
        pcall(function()
            local args = { eggValue, 1 }
            game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("EggService"):WaitForChild("RF"):WaitForChild("TryOpenEgg"):InvokeServer(unpack(args))
        end)
    end

    -- Dropdown for egg selection (Multi)
    local eggDropdown = MainTab:Dropdown({
        Title = "Select Eggs",
        Desc = "Choose which eggs to auto-hatch (you can select multiple)",
        Values = eggsList,
        Value = { "Basic Egg" },
        Multi = true,
        AllowNone = false,
        Callback = function(option)
            selectedEggs = {}
            for _, eggName in ipairs(option) do
                table.insert(selectedEggs, eggsValues[eggName])
            end
            WindUI:Notify({
                Title = "Eggs Selected",
                Content = "Now hatching: " .. table.concat(option, ", "),
                Duration = 3,
                Icon = "egg"
            })
        end
    })

    -- Description reminder
    MainTab:Paragraph({
        Title = "⚠️ Reminder",
        Desc = "The Egg Hatchers is World 1, so please be patient!",
        Color = Color3.fromHex("#222222"),
    })

    -- Toggle for auto hatching
    MainTab:Toggle({
        Title = "Auto Hatch Eggs",
        Desc = "Automatically hatches the selected eggs in order",
        Default = false,
        Callback = function(state)
            autoHatchEggsEnabled = state
            if state then
                if #selectedEggs == 0 then
                    WindUI:Notify({ Title = "Error", Content = "Please select at least one egg first!", Duration = 3, Icon = "alert-triangle" })
                    autoHatchEggsEnabled = false
                    return
                end
                local eggNames = {}
                for _, eggValue in ipairs(selectedEggs) do
                    for name, value in pairs(eggsValues) do
                        if value == eggValue then
                            table.insert(eggNames, name)
                            break
                        end
                    end
                end
                WindUI:Notify({ Title = "Auto Hatch Eggs ON", Content = "Hatching: " .. table.concat(eggNames, ", "), Duration = 3, Icon = "egg" })
                autoHatchEggsThread = task.spawn(function()
                    local currentIndex = 1
                    while autoHatchEggsEnabled do
                        for _, eggValue in ipairs(selectedEggs) do
                            if not autoHatchEggsEnabled then break end
                            hatchEgg(eggValue)
                            task.wait(1) -- Wait 1 second between hatches
                        end
                        task.wait(0.5)
                    end
                end)
            else
                if autoHatchEggsThread then task.cancel(autoHatchEggsThread) autoHatchEggsThread = nil end
                WindUI:Notify({ Title = "Auto Hatch Eggs OFF", Content = "Stopped hatching eggs", Duration = 2, Icon = "egg-off" })
            end
        end
    })

    -- ========== MISC TAB ==========
    local MiscTab = Window:Tab({ Title = "Misc", Icon = "wrench" })

    -- Fly Section
    MiscTab:Section({ Title = "Fly" })

    local flySpeed = 50
    local flyEnabled = false
    local flyBodyVelocity = nil
    local flyBodyGyro = nil
    local flyLoopThread = nil

    local function startFly()
        local char = player.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        local hum  = char:FindFirstChild("Humanoid")
        if not root or not hum then return end

        hum.PlatformStand = true

        flyBodyVelocity = Instance.new("BodyVelocity")
        flyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
        flyBodyVelocity.MaxForce = Vector3.new(1e9, 1e9, 1e9)
        flyBodyVelocity.Parent = root

        flyBodyGyro = Instance.new("BodyGyro")
        flyBodyGyro.MaxTorque = Vector3.new(1e9, 1e9, 1e9)
        flyBodyGyro.D = 100
        flyBodyGyro.CFrame = root.CFrame
        flyBodyGyro.Parent = root

        flyLoopThread = task.spawn(function()
            while flyEnabled do
                local char = player.Character
                if not char then break end
                local root = char:FindFirstChild("HumanoidRootPart")
                if not root then break end
                local camera = workspace.CurrentCamera
                local dir = Vector3.new()

                if UserInputService:IsKeyDown(Enum.KeyCode.W) then
                    dir = dir + camera.CFrame.LookVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then
                    dir = dir - camera.CFrame.LookVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then
                    dir = dir - camera.CFrame.RightVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then
                    dir = dir + camera.CFrame.RightVector
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
                    dir = dir + Vector3.new(0, 1, 0)
                end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) or
                   UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
                    dir = dir - Vector3.new(0, 1, 0)
                end

                if dir.Magnitude > 0 then
                    flyBodyVelocity.Velocity = dir.Unit * flySpeed
                else
                    flyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
                end

                flyBodyGyro.CFrame = camera.CFrame
                task.wait()
            end
        end)
    end

    local function stopFly()
        if flyBodyVelocity then flyBodyVelocity:Destroy() flyBodyVelocity = nil end
        if flyBodyGyro    then flyBodyGyro:Destroy()    flyBodyGyro    = nil end
        if flyLoopThread  then task.cancel(flyLoopThread) flyLoopThread = nil end

        local char = player.Character
        if char then
            local hum = char:FindFirstChild("Humanoid")
            if hum then hum.PlatformStand = false end
        end
    end

    MiscTab:Input({
        Title = "Fly Speed",
        Desc = "Set fly speed (default: 50)",
        Placeholder = "Enter speed...",
        Numeric = true,
        Finished = true,
        Callback = function(value)
            local num = tonumber(value)
            if num and num > 0 then
                flySpeed = num
                WindUI:Notify({
                    Title = "Fly Speed Set",
                    Content = "Speed: " .. tostring(num),
                    Duration = 2,
                    Icon = "zap"
                })
            end
        end
    })

    MiscTab:Toggle({
        Title = "Enable Fly",
        Desc = "WASD + Space / LCtrl",
        Default = false,
        Callback = function(state)
            flyEnabled = state
            if state then
                startFly()
                WindUI:Notify({
                    Title = "Fly ON",
                    Content = "Speed: " .. tostring(flySpeed),
                    Duration = 3,
                    Icon = "arrow-up"
                })
            else
                stopFly()
                WindUI:Notify({
                    Title = "Fly OFF",
                    Content = "Fly disabled.",
                    Duration = 2,
                    Icon = "arrow-down"
                })
            end
        end
    })

    player.CharacterAdded:Connect(function()
        if flyEnabled then
            task.wait(1)
            startFly()
        end
    end)

    -- Movement Speed Section
    MiscTab:Section({ Title = "Movement Speed" })

    local moveSpeed = 16
    local moveSpeedEnabled = false
    local moveSpeedConn = nil

    MiscTab:Input({
        Title = "Movement Speed",
        Desc = "Set walk speed (default: 16)",
        Placeholder = "Enter speed...",
        Numeric = true,
        Finished = true,
        Callback = function(value)
            local num = tonumber(value)
            if num and num > 0 then
                moveSpeed = num
                if moveSpeedEnabled then
                    local char = player.Character
                    if char then
                        local hum = char:FindFirstChild("Humanoid")
                        if hum then hum.WalkSpeed = moveSpeed end
                    end
                end
                WindUI:Notify({
                    Title = "Move Speed Set",
                    Content = "Speed: " .. tostring(num),
                    Duration = 2,
                    Icon = "zap"
                })
            end
        end
    })

    MiscTab:Toggle({
        Title = "Enable Move Speed",
        Default = false,
        Callback = function(state)
            moveSpeedEnabled = state

            local function applySpeed()
                local char = player.Character
                if char then
                    local hum = char:FindFirstChild("Humanoid")
                    if hum then
                        hum.WalkSpeed = state and moveSpeed or 16
                    end
                end
            end

            applySpeed()

            if state then
                moveSpeedConn = player.CharacterAdded:Connect(function(char)
                    task.wait(1)
                    if moveSpeedEnabled then
                        local hum = char:WaitForChild("Humanoid")
                        hum.WalkSpeed = moveSpeed
                    end
                end)
                WindUI:Notify({
                    Title = "Move Speed ON",
                    Content = "Speed: " .. tostring(moveSpeed),
                    Duration = 3,
                    Icon = "activity"
                })
            else
                if moveSpeedConn then
                    moveSpeedConn:Disconnect()
                    moveSpeedConn = nil
                end
                WindUI:Notify({
                    Title = "Move Speed OFF",
                    Content = "Reset to 16.",
                    Duration = 2,
                    Icon = "minus-circle"
                })
            end
        end
    })

    -- Reset Button
    MiscTab:Section({ Title = "Reset" })
    MiscTab:Button({
        Title = "Reset Position & Health",
        Icon = "refresh-cw",
        Callback = function()
            local char = player.Character
            if not char then
                WindUI:Notify({ Title = "Error", Content = "Character not found.", Duration = 2, Icon = "alert-triangle" })
                return
            end
            local hum = char:FindFirstChild("Humanoid")
            local root = char:FindFirstChild("HumanoidRootPart")
            if not hum or not root then
                WindUI:Notify({ Title = "Error", Content = "Humanoid or RootPart missing.", Duration = 2, Icon = "alert-triangle" })
                return
            end

            hum.Health = hum.MaxHealth

            local destination = workspace.Break_Map and workspace.Break_Map.Locations and workspace.Break_Map.Locations.CommonFloorBest
            if destination then
                root.CFrame = destination.CFrame + Vector3.new(0, 5, 0)
                WindUI:Notify({ Title = "Reset", Content = "Teleported to Common Floor & fully healed.", Duration = 3, Icon = "check-circle" })
            else
                WindUI:Notify({ Title = "Warning", Content = "CommonFloorBest not found, only healed.", Duration = 3, Icon = "alert-triangle" })
            end
        end
    })

    -- ========== SUPPORT TAB (with Discord) ==========
    local SupportTab = Window:Tab({ Title = "Support", Icon = "heart" })

    local InviteCode = "nqHTPDVBQg"
    local DiscordAPI = "https://discord.com/api/v10/invites/" .. InviteCode .. "?with_counts=true&with_expiration=true"

    local function httpGet(url)
        local body, success = nil, false
        if syn and syn.request then
            success, body = pcall(function() return syn.request({ Url = url, Method = "GET" }).Body end)
        elseif http and http.request then
            success, body = pcall(function() return http.request({ Url = url, Method = "GET" }).Body end)
        elseif request then
            success, body = pcall(function() return request({ Url = url, Method = "GET" }).Body end)
        elseif http_request then
            success, body = pcall(function() return http_request({ Url = url, Method = "GET" }).Body end)
        end
        if success and body then return body else return nil end
    end

    local Response, raw = {}, httpGet(DiscordAPI)
    if raw then pcall(function() Response = HttpService:JSONDecode(raw) end) end

    local onlineCount = Response and Response.approximate_presence_count or "?"
    local totalCount  = Response and Response.approximate_member_count  or "?"
    local guildName   = (Response and Response.guild and Response.guild.name) or "ZyroYonk Hub"
    local iconUrl     = nil
    if Response and Response.guild and Response.guild.id and Response.guild.icon then
        iconUrl = "https://cdn.discordapp.com/icons/"
            .. tostring(Response.guild.id) .. "/"
            .. tostring(Response.guild.icon) .. ".png?size=256"
    end

    SupportTab:Section({ Title = "Discord Server" })

    local cardProps = {
        Title         = guildName,
        Desc          = "Online: " .. tostring(onlineCount) .. "   Members: " .. tostring(totalCount) .. "\n\nJoin for updates & support.",
        Color         = Color3.fromHex("#222222"),
        ImageSize     = 64,
        ThumbnailSize = 100,
        Buttons = {
            {
                Title = "Copy Invite",
                Callback = function()
                    copyToClipboard("https://discord.gg/" .. InviteCode)
                    WindUI:Notify({ Title = "Copied!", Content = "Invite link copied.", Duration = 2, Icon = "clipboard-check" })
                end
            },
            {
                Title = "Join Discord",
                Callback = function()
                    if openUrl then
                        openUrl("https://discord.gg/" .. InviteCode)
                    else
                        copyToClipboard("https://discord.gg/" .. InviteCode)
                        WindUI:Notify({ Title = "Link Copied", Content = "Open in your browser.", Duration = 3, Icon = "globe" })
                    end
                end
            }
        }
    }
    if iconUrl then cardProps.Image = iconUrl end
    SupportTab:Paragraph(cardProps)

    SupportTab:Section({ Title = "Help" })

    SupportTab:Paragraph({
        Title = "Executor",
        Desc  = "Using: " .. executorName .. "\nReport issues in Discord.",
        Color = Color3.fromHex("#222222"),
    })

    SupportTab:Button({
        Title = "Report a Bug",
        Icon  = "bug",
        Callback = function()
            copyToClipboard("Bug Report\nExecutor: " .. executorName .. "\nScript: ZyroYonk Hub v1.0.6\n\nDescribe the issue:")
            WindUI:Notify({ Title = "Template Copied", Content = "Paste in Discord support channel.", Duration = 4, Icon = "message-square" })
        end
    })
end

-- ================== STARTUP ==================
showLoadingScreen()
task.wait(2.5)
showGUI()
